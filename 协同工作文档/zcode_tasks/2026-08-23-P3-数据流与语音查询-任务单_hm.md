# zcode 任务单：P3 数据流 + 语音查询（BOX-3 上报 + 真机端到端验收）

- **下达方**：Hermes（审查）· 2026-08-23 · P2 已 REVIEW_PASS 闭环
- **状态**：待 zcode 实施（**服务端已就绪，任务收敛为固件上报 + 端到端验收**）
- **依据**：`规划文档/Spec文档/2026-08-19-BLE健康监测与家庭预警-立项-spec_hm.md` P3 路径 + §4.1 DATA 路由

## 重要现状（Hermes 实测核查，2026-08-23）

**服务端 P3 数据链路已完整**（zcode 凌晨"P3 DATA 路由骨架"4ab86d2 已搭好，本次核查确认）：

| 组件 | 位置 | 状态 |
|---|---|---|
| `POST /api/v1/health/data` 接收端点 | `voice-bridge/app/main.py:201-206` | ✅ 已实现（hr/spo2/seq → HealthDataStore） |
| HealthDataStore 缓存 + 阈值状态机 | `app/health.py` | ✅ 已实现（夜间心率下限/超阈值计数/冷却） |
| DATA 路由（优先级高于 RAG 防截胡） | `app/router.py:108-113` | ✅ 已实现（心率/血氧核心词 → data） |
| 模板直答（不经 LLM，确定性） | `app/pipeline.py:128-150, 190, 231` | ✅ 已实现（报数 + 正常/偏高/偏低 + 新鲜度判断） |
| SYSTEM_PROMPT 条件化 | `app/llm.py:50` | ✅ 已实现（"已接入真实监测"而非"即将上线"） |
| `GET /api/v1/health/alert` 预警轮询端点 | `app/main.py:208+` | ✅ 已实现（P4 用，本轮顺带确认） |

**真正缺失（本次任务范围）**：
1. **BOX-3 固件上报**：P2 的 `ble_central.c` 收到 8 字节帧后，**未 POST 到 voice-bridge `/api/v1/health/data`**——数据只缓存在固件内存（`ble_central_get_data()` 供 P3 查询），没上行；
2. **真机端到端验收**：语音问「我的心率多少」→ 走 DATA 路由 → 模板直答真实值（整条链路未实测）。

## 实施内容（BOX-3 固件 `Code文档/固件/box3_voice_agent/` + 源工程 `D:\esp-box\examples\voice_agent\`）

1. **数据上报任务**：`ble_central.c` 帧解析后，把最新 HR/SpO2/seq 经 **HTTP POST `http://<voice-bridge-ip>:8710/api/v1/health/data`**（JSON `{"hr":N,"spo2":N,"seq":N}`）上报；
   - 节奏：**每 5s 一帧（与节点上报同频）** 或帧更新即上报（去抖 5s）——建议帧到即报，服务端幂等；
   - 复用现有 HTTP client（voice_agent.c 已有 WiFi HTTP 能力，参考健康端点轮询/voice 上传代码）；
   - **失败静默**（网络抖动/服务端未启）：重试 1 次，不阻塞 BLE 收帧；
   - **首字红线**（agent.md 最高铁律）：上报任务独立后台，**不得进入语音首字路径**；
2. **阈值预警轮询（P4 预埋，可选）**：`GET /api/v1/health/alert` 每 30-60s 空闲轮询——**本任务不做**（P4 范围），仅在 README 记录设计；
3. **真机验收**（与腕部节点联调）：
   - 腕部节点戴手指 → BOX-3 连接收帧 → 上报 → 服务端 health_store 有数据；
   - 语音「我的心率多少」→ **模板直答真实值**（如「心率 78，正常」）——非 RAG 静态科普、非"即将上线"；
   - 腕部节点摘下 → 语音问 → 「检测暂时中断」（新鲜度过期）；
   - 回归：语音普通对话/搜索/记忆均不受影响。

## 验收（Spec P3 核心）

1. 语音「我的心率多少」→ 返回真实心率（非「即将上线」、非 RAG 科普）——**本任务最关键验收**；
2. 「血氧多少」→ 真实血氧值；
3. 摘腕带后问 → 「检测暂时中断了，请检查腕带是否佩戴好」；
4. 上报链路日志可见（BOX-3 POST 200 + voice-bridge 收到更新）；
5. 语音对话/搜索/记忆无回归（62 passed 服务端测试 + 真机语音对话）；
6. DATA 路由命中日志 `route=data (health data keyword: xxx)`。

## 备注

- 服务端**零改动**（除非验收暴露缺陷，才走任务单返工流程）；
- 产出后送 Hermes 审查（deepseek-v4-pro）；
- P4（预警三路：阈值分析 + BOX-3 播报 + 微信推送 + 电脑通知）依赖本任务的上报链路 + `GET /health/alert`（已就绪）。

*Hermes（deepseek-v4-pro）· 2026-08-23 · 后缀 _hm 遵循 agent.md §5.1*
