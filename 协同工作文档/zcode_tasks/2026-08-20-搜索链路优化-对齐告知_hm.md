# zcode 对齐：国外搜索链路优化已完成（无需代码改动）

- **告知方**：Hermes · 2026-08-20 · 依据 `协同工作文档/完成报告/2026-08-20-国外搜索链路优化-完成报告_hm.md`
- **性质**：知悉对齐，**voice-bridge 侧无待办**；仅 Hermes 侧配置变更

## 结论

慢路径搜索延迟 **73s → 9.8s（完整链路）**，其中 Tavily 搜索本身 **3.3s**，已达 Vange 目标（≤5s）。Vange 拍板维持 Tavily，不切 DDG。

## 变更内容（Hermes 侧，zcode 无需动）

1. `~/.hermes/gateway-service/Hermes_Gateway.vbs`：清空代理 → 选择性代理（`HTTP(S)_PROXY=127.0.0.1:7800` + NO_PROXY 白名单保 DeepSeek/iLink/内网直连）；
2. `~/.hermes/config.yaml` `web.backend: tavily`（保持不变）。

## 影响面确认（zcode 若遇以下现象属预期）

- BOX-3 语音问天气/新闻：安抚语先行（0.4s）→ 搜索 ~3.3s → 答案，总 ~10s；
- Clash Verge 关闭/崩溃时：web 搜索会失败（诚实兜底「查不了」逻辑触发，不编造）——**这是已知运维限制，不是 bug**；
- DeepSeek/iLink/本地服务不受代理变更影响（NO_PROXY）。

## Spec 状态

需求 2（联网搜索）路线 A 验收标准已达成（真实结果返回），Spec `2026-08-19-状态灯与联网搜索-实现方案_zc.md` 无需再改。

*Hermes（deepseek-v4-pro）· 2026-08-20 · 后缀 _hm 遵循 agent.md v1.7 §5.1*
