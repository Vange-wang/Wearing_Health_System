# zcode 会话交接（线程 N+1）

- **交接日期**：2026-08-22 · 交接人：zcode（智谱 GLM）
- **触发**：Vange 要求开新线程
- **用途**：新线程读本文件即可恢复上下文开工

## 一句话现状

可穿戴健康辅助系统 v0.4 语音桥正常运行（PC 本地），腕部节点固件（ESP32-C3 + MAX30102）软件全部写完并验证通过（BLE 广播/8 字节帧/GATT 服务/心率血氧算法），**但 MAX30102 模块硬件损坏**（LED 小板焊点断裂，按压后双 LED 全灭），需要换模块后继续指尖验证（±3% 对照）。

## 已完成（本线程）

| 任务 | 关键产出 | commit |
|---|---|---|
| 提取层结构化送审 | 完成报告 + Hermes 审查报告（REVIEW_PASS） | 7ed4250 |
| 未入库文件提交 | 自助排查手册 + fix 脚本 + 字段级删除/提取层任务单 | 4047dfd |
| 腕部节点 ESP-IDF 工程 | `ble_wrist_node/` 完整工程（MAX30102 驱动 + 心率血氧算法 + BLE peripheral + WS2812 状态灯） | 未提交 |
| MAX30102 驱动调试 | 解决 4 个硬件级坑：MLED 槽位归零 / SPO2_CONFIG ADC 量程 / LED 电流饱和 / IO_MUX FUN_IE 被关 | — |
| 软件 I2C（bit-bang） | 完全绕开 IDF 硬件 I2C 驱动（新驱动无超时忙等 + legacy 恢复困难），彻底解决 WDT 饿死和总线卡死问题 | — |
| BLE 广播 + GATT 验证 | 手机扫描发现 WH-Wrist01（RSSI=-42），服务布局与协议文档完全一致，通知链路正常 | — |
| voice-bridge 自启动 | `start-voice-bridge.bat` 放入启动文件夹 + 桌面快捷方式「启动语音服务」| — |
| voice-bridge watchdog 失败 | `voice-bridge-watchdog.pyw` 方案失败（无限重启循环），已从启动文件夹移除 | — |

## 未完成（带优先级）

| # | 事项 | 优先级 | 责任 | 说明 |
|---|---|---|---|---|
| 1 | **换 MAX30102 模块 + 指尖验证 ±3%** | P1 | Vange | 模块硬件损坏（LED 小板焊点断），需买新模块（~15 元）；接线不变：SDA→GPIO4, SCL→GPIO5, VIN→3V3, GND→GND, IRD/RD 不接 |
| 2 | **固件归档 Code文档/固件/** | P1 | zcode | 腕部节点固件未入库 |
| 3 | **送 Hermes 审查** | P1 | zcode | 代码 + 实测数据（需新模块验证后） |
| 4 | 云服务器部署调研 | P3 | zcode | 方案已调研完成（见下方），待 P1 闭环后实施 |
| 5 | BOX-3 固件双模式（PC↔云切换） | P3 | zcode | NVS 配置 USE_CLOUD 0/1，长按按键切换，零成本回退 |

## 阻塞点

- **P1 阻塞**：MAX30102 模块硬件损坏，需 Vange 买新模块（~15 元，淘宝/MH-ET LIVE MAX30102 模块）
- 无其他阻塞

## 关键文件（绝对路径）

- 腕部节点固件：`D:\esp-box\examples\ble_wrist_node\`（ESP-IDF v5.2.7，target=esp32c3）
  - `main/main.c` — 主程序（采样/上报/LED 三个 FreeRTOS 任务）
  - `main/max30102.c` — **软件 I2C（bit-bang）** 驱动 + 总线自愈恢复
  - `main/max30102.h` — 驱动头文件
  - `main/hr_spo2.c` — 心率（峰峰间隔法）+ 血氧（R 比率，MAXREFDES117 拟合）
  - `main/hr_spo2.h` — 算法头文件
  - `main/ble_periph.c` — NimBLE peripheral（心率 0x180D + 自定义综合帧 + 设备信息）
  - `main/ble_periph.h` — BLE 头文件
  - `main/ws2812.c` — GPIO8 RGB 状态灯（RMT 驱动）
  - `main/ws2812.h` — RGB 头文件
  - `sdkconfig.defaults` — BLE + NimBLE Kconfig 默认值
  - `CMakeLists.txt` — 工程 CMake（idf_target=esp32c3）
- 构建脚本：`D:\esp-box\build_wrist.py`（编译/烧录，`--flash` 烧录 COM6）
- BLE 数据帧协议：`D:\workbuddy_project\项目\可穿戴健康辅助系统\规划文档\技术验证\2026-08-19-BLE数据帧协议_zc.md`
- BLE 立项 Spec：`D:\workbuddy_project\项目\可穿戴健康辅助系统\规划文档\Spec文档\2026-08-19-BLE健康监测与家庭预警-立项-spec_hm.md`
- voice-bridge：`D:\workbuddy_project\项目\可穿戴健康辅助系统\voice-bridge\`
- 桌面快捷方式：`D:\UserData\86166\KnownFolders\Desktop\启动语音服务.lnk`

## 运行环境备忘

| 项 | 值 |
|---|---|
| voice-bridge | 127.0.0.1:8710（`start-voice-bridge.bat` 启动，日志 logs/voice-bridge.log） |
| mDNS | `start-voice-bridge.bat` 内同时启动，日志 logs/mdns.log |
| memory_server | 127.0.0.1:8781（Hermes 侧，重启入口 `~/.hermes/scripts/memory_server.cmd`） |
| gateway | 127.0.0.1:8780（Hermes 侧，pythonw.exe 自动运行） |
| ESP-IDF | `D:\esp-idf-tools\frameworks\esp-idf-v5.2.7`，构建脚本 `D:\esp-box\build_wrist.py` |
| 烧录端口 | **COM6**（腕部节点 ESP32-C3 SuperMini），**COM5**（BOX-3 ESP32-S3） |
| MAX30102 接线 | SDA=GPIO4, SCL=GPIO5, VIN→3V3, GND→GND, IRD/RD 不接 |
| LED 电流 | 0x0F（3mA），拆盖后光路强需降电流防饱和 |
| 采样率 | 50Hz（SPO2_CONFIG=0x42），减少 I2C NACK 碰撞 |

## 已解决的硬件坑（新模块复现时参考）

1. **MLED_CTRL1 寄存器（0x11）必须显式写 0x21**（SLOT1=RED, SLOT2=IR）——复位后默认 0x00，槽位全禁用→FIFO 永远空
2. **SPO2_CONFIG ADC 量程 0x47（4096nA）** 而非 0x27（2048nA），否则量程不够
3. **LED 电流**：拆掉保护盖后光路强，7.2mA（0x24）会让 IR 饱和削顶（ADC 值 262143），降到 3mA（0x0F）
4. **IO_MUX FUN_IE**：IDF 5.2 的 `gpio_config()` 对输出模式会关闭 IO_MUX 输入使能（FUN_IE），导致 `gpio_get_level` 恒读 0（ACK 假阳性）。必须在 `gpio_config` 后调 `gpio_ll_input_enable` 重开输入
5. **硬件 I2C 驱动不可靠**：IDF 5.2 新 `i2c_master` 驱动在设备 NACK 时有无超时忙等缺陷（WDT 饿死）；legacy 驱动恢复后 `i2c_driver_install` 反复失败。最终用**软件 I2C（bit-bang）**彻底解决
6. **MAX30102 LED 小板是排针/焊接连接**，拆保护盖时容易碰松，按压可能导致焊点断裂（本次模块损坏原因）

## 云服务器部署调研结论（P3 待实施）

| 项 | 结论 |
|---|---|
| 推荐方案 | 阿里云/腾讯云学生机（2核2G，~10元/月） |
| 延迟影响 | 不牺牲，x86 ASR 比 PC 更快，网络延迟被抵消 |
| BOX-3 固件改动 | 约 3-5 行：WiFi SSID + 语音服务 IP（NVS 存配置） |
| 回退方案 | NVS 存 USE_CLOUD 0/1，长按按键切换，零成本回退 PC 方案 |
| 风险 | 演示时必须有网络，建议保留 PC 本地方案作备用 |
| 部署耗时 | 约 2 小时 |

## 下一步动作

1. **Vange 买新 MAX30102 模块**（~15 元，淘宝搜「MH-ET LIVE MAX30102」）
2. 新模块到手后接线（SDA→GPIO4, SCL→GPIO5, VIN→3V3, GND→GND, IRD/RD 不接）
3. 烧录现有固件（`D:\esp-box\build_wrist.py --flash`），手指验证心率/血氧
4. 对照指尖血氧仪校准（±3%），必要时调 LED 电流和算法参数
5. 固件归档 `Code文档/固件/` + 送 Hermes 审查

## 规范提醒

- 文件后缀按起草方：Hermes 产出 `_hm`，zcode 产出 `_zc`
- 审查走 Hermes（deepseek-v4-pro），zcode 不自审
- 固件改动先更新归档再编译烧录
- voice-bridge 睡眠后服务被杀 → 双击桌面「启动语音服务」快捷方式恢复

---

*zcode（智谱 GLM）· 2026-08-22 · 后缀 _zc 遵循 agent.md v1.7 §5.1*
