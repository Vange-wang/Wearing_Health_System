# BLE 立项 Spec 校验 审查确认

- **审查方**：Hermes（deepseek-v4-pro）
- **日期**：2026-08-19
- **对象**：`2026-08-19-BLE立项Spec整体校验_zc.md`
- **结论**：**zcode 校验质量高，3 个缺口全部确认并已补进 Spec，4 个风险已处理——Spec 达到可开工状态（REVIEW_PASS，前置完成即开 P1）。**

---

## 一、校验质量评价

对照代码现状实测（端点 grep、RAG 截胡实锤、提示词行号、路由配置）+ 内部一致性（验收↔测试映射）+ 与预研交叉核对——方法论扎实。3 个缺口都是实锤（有证据），不是猜测。

## 二、缺口处理（已补进 Spec）

| 缺口 | 裁决 | 处理 |
|---|---|---|
| 1. 预警①下行通道未定义 | **采纳方案 A**（BOX-3 空闲轮询 `GET /api/v1/health/alert` 每 30-60s）| 阈值单点在服务端可配，固件只加轮询改动最小；预警延迟=轮询间隔（连续监测可接受）→ 已补 §5.1 |
| 2. RAG 截胡（实测实锤） | **新增 DATA 路由**（优先级高于 RAG）+ **模板直答**（确定性，不经 LLM）+ SYSTEM_PROMPT 第 5 条条件化 | 已补 §4.1（P3 硬前置） |
| 3. 微信配额不闭环（最坏 1.7h 耗尽全天配额） | **微信日上限 ≤5 条** + 超限降级（只走 BOX-3+电脑，次日补报）| 已补 §5.4 |

## 三、风险处理（已写进 Spec）

| 风险 | 处理 |
|---|---|
| 心率 <50 夜间误报（老人睡眠 45-55 常见） | 夜间（23:00-6:00）下限下调 <45 → 已补 §5.1 阈值表 |
| BLE 字节级协议未定义 | P1 前置：UUID/字段/字节序/采样周期固化为 P1/P2 契约 → 已补 §9 |
| 固件代码未入库（voice_agent.c 在 git 外） | P1 前置：归档 `Code文档/固件/` → 已补 §9 |
| 数据新鲜度无验收线 | >5min 答「检测中断」，>30min 预警自禁用 → 已补 §6 第 7 条 |

## 四、结论

**REVIEW_PASS（缺口已补，可开工）**：
1. Spec 更新完成（§4.1 DATA 路由 + §5.1 下行通道/夜间阈值 + §5.4 日上限 + §6 新鲜度 + §9 前置）。
2. P1 开工条件：硬件到位（ESP32-C3 + MAX30102）+ zcode 完成两个前置（固件入库 + BLE 协议文档）。
3. P3 硬前置（DATA 路由）已设计，P3 动工前无需再返工设计。

---

*Hermes（deepseek-v4-pro）· 2026-08-19*
