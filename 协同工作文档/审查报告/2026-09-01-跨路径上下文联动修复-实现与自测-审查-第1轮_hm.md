# 2026-09-01 跨路径上下文联动修复 实现与自测 审查 第1轮

- **审查方**：Hermes（deepseek-v4-pro / provider=deepseek）· 2026-09-01 · 后缀 `_hm`
- **Session**：`20260901_004431_11dfd7`（沿用当前专用 session，未新建）
- **状态**：审查完成，等待 Vange 裁决；ISSUE-0013 保持 Open
- **审查对象**：ISSUE-0013 跨路径上下文联动修复（Spec + 任务单 + 4 个实现/测试文件 + 2 份 _cdx 报告）

---

## 0. 结论（verdict）

**PASS_WITH_NONBLOCKING_OPEN_ISSUES**

- SERIOUS：**0 项**。实现行为、范围边界、首字安全、失败/并发语义均符合 Spec；无行为错误、无越界、无首字风险、无假证据、无失败/并发安全问题。
- NON_SERIOUS：**3 项**（OI-1~OI-3），均不阻塞真机门，登记为 Open Issues 后放行。
- 本地门已足以放行；真机门（1484.589ms 全链路首字）尚未执行，**不得**据此关闭 ISSUE-0013。

---

## 1. 冻结证据独立复核（全部通过）

| 项 | 冻结值 | 独立复核 |
|---|---|---|
| Git HEAD | `ff3ab942997b46a5beffb4daa76a66572adba7ef` | ✓ 一致，未 commit/push/reset/clean |
| router.py SHA256 | `35371BA2402EEFC035BC2B0B5CADBE0777402DE0849F9AC0FC27D8B6F68F3724` | ✓ 一致 |
| pipeline.py SHA256 | `E27684BD229D7E567C2F20FC45CD14377AC9CD2118CA052BB37ECF7AFAE967FE` | ✓ 一致 |
| test_router.py SHA256 | `E4288F3716653AFB2DA4F6F3B1BC5167880CF519B6D2FC755E9C2020656CC522` | ✓ 一致 |
| test_pipeline.py SHA256 | `E09BA5739DC4A437AD361BCB53B99514F7D0DB480FC5CC18FDCA7C6D5D4CFB6B` | ✓ 一致 |
| Spec SHA256 | `853557DD27DA6FBD3DDF60E33F0819C0170C91985E4AED6A28046AE21C020AD0` | ✓ 一致 |
| 任务单 SHA256 | `4BEE80ADFF2798F2F82379569BB76DEC35F15D2E8F44254CC8D3FBF9F3AF4DE5` | ✓ 一致 |

- 全量复跑：`venv/Scripts/python.exe -m pytest tests -q` → `193 passed, 5 skipped, 1 warning in 82.38s`，与 _cdx 报告一致。
- 定向复跑：`tests/test_router.py tests/test_pipeline.py` → `31 passed`（13 router + 18 pipeline）。
- 唯一 warning 为既有 Starlette/httpx 弃用提示（fastapi/testclient），本任务未引入。

---

## 2. 逐项核查（A–H）

### A. phrase-level 分类与负例、ISSUE-0010 保持 — PASS

- `Router.classify_followup()`（router.py:97-107）仅匹配 `REPLAY_PHRASES`（8 个完整复合短语，router.py:65-68）与 `CONTEXT_PHRASES`（6 个，router.py:70-73），无单词宽泛命中。
- 负例 `查询一次天气/说一遍注意事项/再查一次天气/说一下天气/讲个笑话/谢谢` 均判 None（test_router.py:119-126）。
- ISSUE-0010 `_is_followup` 与 `FOLLOWUP_HINTS` 完全未改（router.py:22、217-227），分层独立成立。

### B. 快照 4 字段 / TTL / 仅一轮 / 锁 / 提交规则 — PASS

- 字段 `last_user_text/last_reply_text/last_route/last_ts`（pipeline.py:179-184）。
- TTL `SNAPSHOT_TTL_SECONDS=600`（pipeline.py:27），`_read_last_round` 以 monotonic 判过期并就地置空（pipeline.py:163-173）。
- 仅保留最近一轮，无历史列表。
- 锁只包 O(1) 字典复制/替换（`_read_last_round` 返回浅拷贝；`_commit_last_round` 锁内仅一次赋值），锁内无 `await`、无 I/O/LLM/TTS。
- 提交规则闭合（pipeline.py:711-717）：普通成功轮按 `timing["route"]` 覆盖；成功 CONTEXT 按 `last_round["last_route"]` 覆盖；REPLAY/无快照回退不提交、不刷新 ts；失败/取消因 `delivery_failed`/异常到达不了提交点。
- `SNAPSHOT_ROUTES`（pipeline.py:29）保证 `last_route` 仅允许 LIGHTWEIGHT/RAG/HERMES/DATA；`replay`/`context` 只进 `timing["route"]` 观测字段，不落快照。

### C. REPLAY 独立执行 — PASS

- replay 分支（pipeline.py:328-338）：`route="replay"`、`response_route="replay"`、`selected_llm=None`、`final_text=reply_text`、`llm_backend="replay"`、`delta_iter=iter([final_text])`。
- 不调 `rag.search`（仅普通路由 else 分支调用，pipeline.py:370-375）、不调 `stream_chat`（`local_followup` 走模板流，pipeline.py:425-426）、不排 Hermes comfort（`_produce` 判断 `response_route == HERMES`，pipeline.py:567，replay 为 "replay" 不触发）。
- 无快照 replay → `REPLAY_NO_SNAPSHOT_TEXT`，`llm_backend="template"`，零 LLM/网络。

### D. CONTEXT 四路闭合 — PASS

- HERMES→`self.llm` 单次（pipeline.py:354-357）；LIGHTWEIGHT→`lightweight_llm` 单次（pipeline.py:358-367）；RAG→不重检不重包、单次 lightweight（pipeline.py:358-367）；DATA→`_build_context_data_reply()` 本地重报、零 LLM（pipeline.py:349-353）。
- 前缀固定结构 `【上一轮】用户/小V/【本轮】用户`，reply 截断 `[:CONTEXT_REPLY_MAX_CHARS=500]`，仅一轮，不写日志（pipeline.py:188-195）。
- 轻量降级保留上下文：LLMError 回退 Hermes 时 `fallback_text = final_text if context_followup else text`（pipeline.py:437），前缀不丢，test_pipeline.py:390-403 已验证。

### E. 失败 / 并发安全 — PASS

- `delivery_failed[0]` 在 TTS 部分/全部失败、空句、worker 异常处置位（pipeline.py:528/534/539/553），提交门 `not delivery_failed[0] and total_frames > 0`（pipeline.py:711）保证失败不写脏快照。
- LLM 中断（异常传播，到达不了提交点）、取消（CancelledError 传播，无提交）均保留旧快照，test_pipeline.py:313-418 覆盖。
- 并发：锁 + 浅拷贝保证无字段撕裂，test_pipeline.py:439-456 覆盖；锁内无 await，无死锁；无新增 task 泄漏路径（preconnect 收尾沿用既有逻辑）。
- 首帧：快照读仅一次 O(1) 锁 + 字典拷贝，位于路由前，不阻塞首帧；本地 N=10 证据（REPLAY median 0.260ms < 普通 0.428ms）印证。

### F. 越界 / 敏感日志 / 依赖 — PASS

- git diff 仅 3 个受控文件（router.py +24、pipeline.py +172/-19、test_router.py +32）；test_pipeline.py 为精确新建；2 份 _cdx 报告为白名单。
- `voice-bridge/pytest.ini` mtime=2026-08-31，为上一任务既有未跟踪文件，非本任务新建（复核不属越界）。
- 新增仅 stdlib `import threading`；无新依赖/API/配置；无新增敏感全文日志（`_build_context_prompt` 明确不负责日志，流式日志仍截断 `[:20]`）。

### G. 测试质量 — PASS（2 处 NON_SERIOUS 覆盖缺口见 OI-1/OI-2）

- 通过公共接缝 `Router.classify_followup` 与 `StreamingPipeline.run_text` 验证行为，非测实现细节；TDD RED/GREEN 记录完整；关键边界（TTL 过期、不刷新、覆盖、失败、取消、并发、轻量降级保前缀）均有断言。

### H. 首字证据与门边界 — PASS（本地门放行，真机门未执行）

- 本地门：REPLAY median/p95=0.260/0.318ms ≤ 普通 LIGHTWEIGHT 0.428/0.464ms，额外 LLM 调用=0，满足 +50ms；三条普通路径修改前后 median/p95 变化均亚毫秒，无回退。
- 真机门 1484.589ms 未执行，正确保持 ISSUE Open；_cdx 报告第 9 节与本报告结论一致，未把本地门当成交付完成。

---

## 3. 问题批次

### SERIOUS — 无

### NON_SERIOUS（Open Issues，不阻塞真机门）

| ID | 级别 | 位置 | 证据 | 影响 | 最小修正 | 关闭条件 |
|---|---|---|---|---|---|---|
| OI-1 | NON_SERIOUS | test_pipeline.py:87-102 | REPLAY 用例断言 route/llm_backend/无 LLM/无 RAG，但未断言 `timing["comfort_sent"] is False` 或帧流无 comfort 帧 | Spec S1「不排 HERMES comfort」实现正确（response_route="replay"≠HERMES）但缺回归守卫 | replay 用例补 `assert timing["comfort_sent"] is False` | 补断言后该用例通过 |
| OI-2 | NON_SERIOUS | pipeline.py:473-540 | 测试 TTS fake 均走非流式 `synthesize`，新增的流式 `stream_synthesize` 失败→`delivery_failed` 逻辑（`_drain_stream` 返回 completed）未被覆盖 | 快照完整性的流式失败路径无回归保护（生产 edge-tts 走 stream_synthesize） | 补一个 `stream_synthesize` 中途 TTSError 的 TTS fake，验证 `delivery_failed=True`→不提交快照 | 补用例后通过 |
| OI-3 | NON_SERIOUS | pipeline.py:197-204、711-717 | CONTEXT+DATA 数据不新鲜时 `_build_context_data_reply` 返回 `CONTEXT_NO_SNAPSHOT_TEXT`，但该降级轮仍按「成功 CONTEXT」提交快照（route=DATA） | 后续 REPLAY 会重播「我有点没跟上…」，属 TTL 600s 内边界语义退化 | CONTEXT 轮若 `final_text == CONTEXT_NO_SNAPSHOT_TEXT` 则不提交快照（或在 DATA-context 分支显式区分「真实重报」与「降级」） | 补「DATA-stale 后 REPLAY 不重播降级文本」用例并通过 |

---

## 4. 本地门与真机门边界（不可混写）

- **本地门（已满足，可放行本地）**：内存 Fake + 零网络，N=10，median/p95，REPLAY `first_frame_ready_ms ≤ 普通 LIGHTWEIGHT + 50ms` 且额外 LLM 调用=0；三条普通路径无回退。
- **真机门（未执行，不可替代）**：BOX-3 全链路首字 ≤ 1484.589ms + 生日复述/慢路径指代追问不答非所问；由 Vange 安排，是 ISSUE-0013 四段关闭门之一。
- 本报告不将本地门等同于真机门，不据此关闭 ISSUE-0013。

---

## 5. 越界写入检查

- 审查期间仅允许写入本报告文件 `协同工作文档/审查报告/2026-09-01-跨路径上下文联动修复-实现与自测-审查-第1轮_hm.md`。
- 未修改任何实现/测试/Spec/任务单/_cdx 报告/配置/依赖/Git/固件/设备；未 commit/push/reset/clean；未烧录/重启/采样；未发起网络业务请求。

---

## 6. 唯一下一动作

登记 OI-1~OI-3 至 `协同工作文档/审查报告/Open-Issues.md`，由 Vange 裁决是否放行真机门；放行后由 Vange 安排 BOX-3 真机语音与 1484.589ms 全链路首字验收，四段门全部满足后才关闭 ISSUE-0013。

---

*Hermes（deepseek-v4-pro）· 2026-09-01 · 后缀 _hm 遵循 agent.md §5.1*
