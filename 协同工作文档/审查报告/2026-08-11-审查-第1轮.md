# 审查报告 — 2026-08-11-审查-第1轮（第1轮/共3轮）

> 任务单：T-20260811-03 · 审查方 Hermes（deepseek-v4-pro）· Spec v0.1.1 · commit cc07d97
> 交付范围：voice-bridge v0.1 线A电脑侧代码（app 七文件 + run.py + config.yaml + requirements.txt + tests + 自测报告）

## 审查结论

**REVIEW_CONDITIONAL_PASS** ✅（第 1 轮 / 共 3 轮）

- SERIOUS（阻断）：**0 项**
- NON_SERIOUS（非阻断）：**7 项**（已登记 Open-Issues.md，编号 OI-001 ~ OI-007）
- 遗留项均为环境适配/文档/非功能性优化，不阻断 v0.1 功能交付

**核心理由**：抽象接口可插拔、Spec §5 接口全实现、Spec §6 系统提示词一字不差、打点计时完整、错误处理逐级映射、直连 DeepSeek（trust_env=False）——代码质量与 Spec 合规性均达到 v0.1 放行标准。D1~D6 偏差均为外部环境约束的合理工程应对，非代码缺陷。

---

## 一、Spec 合规逐条核查

| Spec 条款 | 要求 | 实现 | 结论 |
|---|---|---|---|
| §3 技术栈 | Python 3.11 / FastAPI / sherpa-onnx / edge-tts+piper / DeepSeek 直连 / venv 内安装 | 全部符合；trust_env=False 保证直连 | ✅ |
| §4 目录结构 | app/{main,config,asr,tts,llm,schemas,\_\_init\_\_}.py + run.py + config.yaml + requirements.txt + README | 七文件齐全；tests/ 测试目录存在 | ✅ |
| §4 抽象接口 | ASR/TTS/LLM 接口与实现分离，换引擎只改 config | ASRBase/LLMBase/TTSBase + 工厂函数 create_* | ✅ |
| §5 GET /api/v1/health | 返回 status/asr/tts，预加载模型报告状态 | 实现正确；tts 字段仅返回主引擎名（见 OI-001） | ⚠️ |
| §5 POST /api/v1/voice/chat | multipart WAV → ASR → LLM → TTS → WAV bytes + X-Timing | 实现完整，打点计时正确 | ✅ |
| §5 错误处理 | 400/no_speech, 400/bad_audio_format, 502/upstream_error | 全部实现；额外增加 413/audio_too_long, 500/config_error, 503/service_unavailable（合理扩展） | ✅ |
| §5 结构化日志 | 每请求一条 {ts,asr_ms,llm_ms,tts_ms,total_ms,asr_text_len,tts_text_len} | JSON 格式，字段完整 | ✅ |
| §6 系统提示词 | 小衡人设，≤50字，口语化，健康数据回"即将上线" | `SYSTEM_PROMPT` 逐字匹配 | ✅ |
| §7 config.yaml | server/asr/tts/llm/log 结构 | 结构一致；新增 piper_model/piper_config 字段（合理扩展，piper 需模型路径） | ✅ |
| §8 模型获取 | SenseVoice + piper 中文模型 | 实际用 sense-voice-zh-en-ja-ko-yue-int8-2024-07-17（D2，资产名不存在，已登记 OI-002） | ⚠️ |
| §9 验收六条 | health/timing/三句实测/断网兜底/交付/测试 | 五条全绿，自测报告实测数据完整 | ✅ |
| §10 运行环境 | venv 在 voice-bridge/venv，run.py 启动 | 符合 | ✅ |

---

## 二、代码审查（逐文件）

### app/main.py（154 行）— ✅ 通过

- 接口路由正确，`@app.on_event("startup")` 预加载模型
- 打点：`time.perf_counter()` 纳秒精度，`X-Timing` 头 JSON 格式正确
- 错误码映射完整：400/413/500/502/503，逐级匹配
- 临时文件 `wav_path.unlink(missing_ok=True)` 在 finally 清理
- **注意**：line 80 TTS 未就绪时 detail 固定写死"TTS 未就绪"，不像 ASR/LLM 用启动时的具体错误信息（OI-003）

### app/config.py（61 行）— ✅ 通过

- 最小 `.env` 解析器（不引入 python-dotenv，遵守不引入未列依赖红线）
- `deepseek_api_key()`：环境变量优先 → .env 兜底，逻辑正确
- 默认值设计合理（端口 8710、model deepseek-chat 等）

### app/asr.py（87 行）— ✅ 通过

- `ASRBase` 抽象接口 + `SherpaOnnxASR` 实现，工厂 `create_asr()`
- `read_wav_16k_mono()` 格式校验完整（channel/sampwidth/rate/duration）
- `audio_too_long` 抛出具体 ValueError（main.py 映射为 413）
- 模型文件检查 `model.int8.onnx + tokens.txt` 存在性

### app/tts.py（145 行）— ✅ 通过

- `TTSBase` 抽象接口 + `EdgeTTS`/`PiperTTS` 实现
- `TTSEngine` 主引擎+兜底模式，失败时日志有 fallback 记录
- `PiperTTS._synthesize_sync` 走 `asyncio.to_thread()` 不阻塞事件循环
- piper 音频重采样用标准库 `audioop`（零新增依赖）
- Windows piper-tts 1.6.0 espeak 打包 bug 的 PEP 环境变量修复到位

### app/llm.py（69 行）— ✅ 通过

- `LLMBase` 抽象接口 + `DeepSeekLLM` 实现
- `SYSTEM_PROMPT` 与 Spec §6 逐字一致
- `httpx.Client(trust_env=False)` 不信任环境代理，保证直连
- `stream=False` 强制非流式（v0.1 阶段边界红线恪守）

### app/schemas.py（15 行）— ✅ 通过

- `HealthResponse` / `ErrorResponse` 模型清晰

### run.py（8 行）— ✅ 通过

- uvicorn 入口，参数从 config 读取

### config.yaml（18 行）— ✅ 通过

- 结构与 Spec §7 一致
- 新增 `piper_model`/`piper_config` 字段为必要扩展（piper 需模型路径）
- 模型目录名使用实际存在的资产名（D2 偏差已记录）

---

## 三、测试审查（tests/test_smoke.py，131 行）

| # | 用例 | 覆盖 | 结论 |
|---|---|---|---|
| 1 | `test_health_format` | health 返回值格式校验（status/asr/tts 字段类型） | ✅ |
| 2 | `test_empty_audio_returns_no_speech` | 空音频 → 400 no_speech（需 ASR 模型） | ✅ skip 条件合理 |
| 3 | `test_bad_format_returns_400` | 非 WAV → 400 bad_audio_format（附加用例） | ✅ |
| 4 | `test_edge_failure_falls_back_to_piper` | mock 主引擎失败 → piper 兜底生效（纯单元） | ✅ 无需模型/网络 |
| 5 | `test_full_chain_x_timing` | X-Timing 头格式（全链路冒烟，需模型+key+网络） | ✅ skip 条件合理 |

- 动态 skip 条件设计良好（缺模型/缺 key 自动跳过，不报 FAIL）
- 5 条全绿（12.21s），覆盖 ≥4 条 Spec 要求
- test_health_format 中 tts 字段预期值 `("edge", "piper", "unavailable")` 与实际返回值一致

---

## 四、偏差裁决（D1~D6）

| 编号 | 偏差内容 | 裁决 | Open Issue |
|---|---|---|---|
| D1 | edge-tts 6.1.x 持续 403（微软封 token），实际 piper 独撑 TTS | **接受**。升 7.x 需转码引入未列依赖，违反红线。现有兜底机制正确，日志完整。v0.2 评估 edge 恢复方案 | OI-004 |
| D2 | ASR 模型资产名 Spec 写 `sherpa-onnx-sense-voice-zh-20240418`，该资产不存在；实际用 `sense-voice-zh-en-ja-ko-yue-int8-2024-07-17`（官方有效资产） | **接受**。Spec 写的资产名不存在，实际用的是官方有效资产。Spec §8 需修正资产名 | OI-002 |
| D3 | piper-tts 1.6.0 Windows espeak 打包 bug（junction 修复） | **接受**。已通过目录联接修复，README 注明。迁移需重建 junction | OI-005 |
| D4 | edge-tts venv vendor patch（communicate.py outputFormat） | **接受**。重装 edge-tts 需重打 patch，已文档化 | OI-006 |
| D5 | SenseVoice 静音幻觉（纯静音识别为噪声字符，不返回空） | **接受**。已用 0 帧 WAV 触发 no_speech 兜底；v0.2 建议加 VAD | OI-007 |
| D6 | ESP-IDF v5.2.7 替代 v5.2.1（无 5.2.1 离线安装器） | **接受**。IDF 环境问题，非 voice-bridge 代码范围；已在 ESP-IDF 安装记录中说明 | OI-004（合并） |

---

## 五、Open Issues 清单（登记于 Open-Issues.md）

| 编号 | 描述 | 严重度 | 责任方 |
|---|---|---|---|
| OI-001 | health tts 字段只返回主引擎名（如 "edge"），edge 不可用时仍显示 "edge"，未反映实际可用引擎 | NON_SERIOUS | WorkBuddy |
| OI-002 | Spec §8 ASR 模型资产名 `sherpa-onnx-sense-voice-zh-20240418` 不存在，修正为 `sense-voice-zh-en-ja-ko-yue-int8-2024-07-17` | NON_SERIOUS | Hermes（Spec 修正） |
| OI-003 | main.py:80 TTS 未就绪时的 detail 固定写死，不像 ASR/LLM 用具体错误信息 | NON_SERIOUS | WorkBuddy |
| OI-004 | edge-tts 主引擎不可用（403），v0.1 实际 piper 独撑；v0.2 需评估恢复方案（edge 7.x+转码 or 替换云 TTS） | NON_SERIOUS | Hermes（v0.2 Spec） |
| OI-005 | piper-tts Windows espeak 打包 bug 依赖目录联接，迁移机器时需重建 | NON_SERIOUS | WorkBuddy |
| OI-006 | edge-tts venv vendor patch 在重装依赖时丢失，需在 README 注明打 patch 步骤 | NON_SERIOUS | WorkBuddy |
| OI-007 | SenseVoice 静音幻觉，建议 v0.2 加 VAD 或能量门限 | NON_SERIOUS | Hermes（v0.2 Spec） |

---

## 六、实测数据核查

自测报告三句实测数据经核查：

| 输入句 | ASR 正确 | LLM 符合人设 | 全链路 delay | 结论 |
|---|---|---|---|---|
| 今天天气怎么样 | ✅ "今天天气怎么样？" | ✅ 口头回应+加衣提示 | asr188/llm2259/tts1854/total4386 | 通过 |
| 帮我查一下心率 | ✅ "帮我查一下心率。" | ✅ "健康数据监测功能即将上线" | asr217/llm584/tts1199/total2058 | 通过 |
| 你叫什么名字 | ✅ "你叫什么名字？" | ✅ "我叫小衡" | asr183/llm453/tts1046/total1732 | 通过 |

- ASR 识别 3/3 全对
- LLM 回复 3/3 符合 Spec §6 系统提示词（简短口语化、心率句精准命中模板、无 markdown）
- 打点数据完整，结构化日志可查
- 延迟范围 1.7~4.4s（v0.1 非流式正常范围；瓶颈在 LLM 完整回复等待，v0.2 流式化后解决）

---

## 七、放行条件

v0.1 代码已达到放行标准。当前交付可在以下条件下合入：

1. OI-001 ~ OI-007 已登记至 `协同工作文档/审查报告/Open-Issues.md`（随附）
2. WorkBuddy 在 v0.2 开工前逐条确认遗留项分配
3. 合入后 git 提交信息使用 `feat: voice-bridge v0.1 — REVIEW_CONDITIONAL_PASS (第1轮/共3轮)`

---

*审查执行：Hermes（deepseek-v4-pro）· 2026-08-11 · agent.md v1.1 合规*
