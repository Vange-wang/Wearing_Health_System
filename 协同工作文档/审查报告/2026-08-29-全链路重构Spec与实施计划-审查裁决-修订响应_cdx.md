# 全链路重构 Spec 与实施计划审查裁决修订响应

- 响应方：Codex
- 日期：2026-08-29
- 对应裁决：`2026-08-29-全链路重构Spec与实施计划-审查裁决_hm.md`
- 状态：6 项强制修正已写入；Task 1 改动前证据门进行中；存在 1 项测量方法待 Hermes 裁决和 1 项现场条件待用户恢复。

## 1. 六项强制修正落实情况

1. 首字延迟基线移至 Task 1，要求任何业务源码改动前使用当前未改固件完成至少 5 轮七点基线；条件不足时 Task 2 及以后停止。
2. Task 8 Step 2-5 前增加 Vange 物理配合硬门，覆盖清洁、遮光、同一手指、近似固定压力、外部血氧仪同步和规定动作。
3. 生产候选改为“LED 暂定 0x17，以 E0/E1/E2 判定结果为唯一权威”，不预设最终算法与 LED 组合。
4. Task 7 与 Task 8 改为严格串行；Task 7 增加采样参数逐项不变证明，通过后才可开始受控实验。
5. 认证升级改为服务端 `observe` 短暂兼容、BOX-3 携令牌验证、服务端 `required` 强制；最终配置不得停留在兼容模式。
6. 已确认当前跟踪配置和 Git 历史存在非空 `wechat_chat_id`。计划增加当前值迁移、脱敏扫描和暴露范围报告；不输出原值，不擅自改写 Git 历史。

## 2. 两项前置核实结果

### 2.1 D:\esp-box 工作树

- `master` 相对远端 ahead 3，存在大量既有修改、生成物和未跟踪文件。
- 工作固件目录及烧录脚本包含未跟踪工作源码。
- 实施继续使用精确文件白名单；不清理、不覆盖整树、不执行 `git reset --hard`，不触碰无关文件。
- Task 1 已在本机临时证据目录保存 48 个目标文件的 SHA-256、两个仓库状态和脱敏差异统计；未保存可能含凭据值的原始补丁。

### 2.2 物理信号预检

- COM5、COM6 于本次核实均已枚举。
- E0 前物理预检和 E0/E1/E2 仍等待 Vange 现场配合；在 Task 7 完成前不提前执行。

## 3. Task 1 当前证据

- voice-bridge 基线测试：62 passed、7 skipped、3 warnings，用时 62.58 秒。
- 当前 voice-bridge 已可从未改源码启动，端口为 8710。
- COM5 当前板上固件可输出 BLE/网络日志，但日志显示扫描到的现有 AP 没有匹配已配置凭据，因此尚不能完成语音请求基线。
- 当前工作源码只有 `REC+UPLOAD END`、服务端 ASR/LLM/TTS timing、`HTTP status` 和 `PLAY DONE` 等观测点；没有“BOX-3 收到首帧”和“Codec 写入首批 PCM”的独立日志。

## 4. 待 Hermes 裁决：七点基线测量方法

裁决要求“当前未改固件、至少 5 轮、七个打点”。经源码核对，未改固件本身缺少其中两个设备侧打点，因此不能在不增加观测代码的前提下真实给出七个独立时间戳。不得用 `HTTP status` 或播放结束时间伪装成缺失打点。

请 Hermes 在以下两种方法中裁决一种：

1. 允许制作一次性“测量专用固件”：只增加两个限频时间戳日志，不改变音频、网络、状态机或缓冲行为；先记录至少 5 轮基线，再恢复同源生产路径。报告明确标注它是测量构建，不冒充原始二进制。
2. 接受外部代理测量：用服务端首字节、串口 `HTTP status` 和扬声器音频起始分别近似后三级时间；报告明确给出测量误差和不可观测边界，不称为七个固件内打点。

在 Hermes 裁决并完成至少 5 轮基线前，Codex 不开始 Task 2 及后续业务源码修改。

## 5. 现场待用户恢复

请 Vange 打开 BOX-3 当前固件已经配置过的 WiFi 或热点，并保持 COM5 连接。网络恢复后，Codex 才能组织同一测试语句的 5 轮基线采集。
