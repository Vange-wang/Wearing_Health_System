# 长期 RAG 自测报告 审查报告

- **审查方**：Hermes（大脑）/ deepseek-v4-pro
- **日期**：2026-08-16（凌晨）
- **审查对象**：`Code文档/长期RAG自测报告.md`
- **依据 Spec**：`规划文档/Spec文档/2026-08-15-语音桥-RAG立项-spec.md` §6/§7
- **结论**：**REVIEW_CONDITIONAL_PASS**——核心目标达成（延迟 3.5s→1.5s 实证），3 项非阻断遗留登记 OI。

---

## 一、结论

长期 RAG 核心目标达成：
- **延迟压回 ~1.4s**（实测稳态 1310~1473ms ≤2000ms 达标线），对比 v0.3 慢路径 3.5s，约 2.4 倍改善，与 v0.2 裸 DeepSeek 持平——验证「轻量通道 ≈ 裸模型延迟」成立。
- 共用记忆（USER.md 注入）、知识库 RAG、慢路径不回退、热重载、错误降级全部验证通过。

---

## 二、验收核对（Spec §6 八条）

| # | 验收项 | 结果 |
|---|---|---|
| 1 | 纯闲聊走轻量 + open_ms ≤2000ms | ✅ 稳态 1310~1473ms |
| 2 | 轻量通道共用记忆（USER.md 注入） | ✅ T1 单元实证（「蓝色」进 prompt） |
| 3 | 知识库查询命中 RAG | ✅ T3/T4（BM25 命中 + 内容注入） |
| 4 | 慢路径不回退（Hermes + 安抚语） | ✅ v0.3 回归通过 |
| 5 | 路由正确（三分分类） | ✅ T2 单元 |
| 6 | 错误路径（DeepSeek 不可达 502 / USER.md 失败降级） | ✅ 降级实测 |
| 7 | 知识库热重载 | ✅ T9 |
| 8 | 结构化日志 route=lightweight/rag/hermes | ✅ |

测试门：**21 passed + 7 skipped**（T7/T8 人工验收 + v0.2 T10 退役）。

---

## 三、遗留（非阻断，登记 OI）

1. **T7 微信→语音端到端共用记忆**：机制已由 T1 + v0.3 前置查证证实，但「微信真实写入 → 语音真实读到」的端到端交叉从未真跑，一直留人工。
2. **T8 慢路径行为级回归**：技能触发真机验证留人工。
3. **BM25 无语义**（"胸口疼"↛"胸痛"）：语义增强 bge-onnx 留后续单独立项（Spec §9 已列风险）。

---

## 四、审查意见（一个必须闭环的点）

**T7 是 Spec §6.2 标「核心」的验收项，但从 v0.3 一直「留人工验收」到现在，始终没有端到端闭环。**

机制层面（USER.md 只读注入）已由 T1 单元 + v0.3 前置查证双重证实，但「端到端真跑一次」是核心验收的闭环证据，不该无限期挂「人工」。

**建议（成本极低，尽快做掉）**：
1. 微信里对 Hermes 说一句「记住我喜欢蓝色」→ 确认 USER.md 新增该条。
2. 语音问「我喜欢什么颜色」→ 轻量通道（DeepSeek + USER.md）应答出「蓝色」。
3. 两侧截图/日志留证，T7 即闭环。

这一步现在 voice-bridge 与微信 gateway 都已就绪，只差「真跑一次」+ 留证，不需要新代码。

---

## 五、踩坑确认（已修复，无遗留问题）

1. `~` 路径未展开（`Path` 不展开 `~`）→ USER.md 读失败静默降级慢路径。已修复 `expanduser()`，且降级有日志、记忆不丢（慢路径 Hermes 自带记忆）。
2. T6 冷启动假阳性 → 已改暖机后测稳态。
3. jieba 首载 ~0.5s → 启动建索引时预热，不摊进请求。

---

## 六、下一步

1. 闭环 T7（§四，微信→语音端到端共用记忆，成本极低）。
2. T8 慢路径行为级回归（技能触发真机验证）择机做。
3. 完成后长期 RAG 正式关闸；语义增强 bge-onnx 作为后续独立项。

*Hermes（deepseek-v4-pro）· 2026-08-16*
