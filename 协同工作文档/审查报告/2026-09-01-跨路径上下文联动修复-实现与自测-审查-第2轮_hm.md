# 2026-09-01 跨路径上下文联动修复 实现与自测 审查 第2轮

- **审查方**：Hermes（deepseek-v4-pro / provider=deepseek）· 2026-09-01 · 后缀 `_hm`
- **Session**：`20260901_004431_11dfd7`（沿用同一专用 session，未新建）
- **状态**：审查完成，等待 Vange 裁决；ISSUE-0013 保持 Open

---

## 0. 结论（verdict）

**PASS_WITH_NONBLOCKING_OPEN_ISSUES**

- SERIOUS：**0 项**。本轮仅测试文件变化，生产代码（router.py / pipeline.py / test_router.py）哈希与 R1 完全一致，无行为改动。
- 本轮新增 SERIOUS：**0**；新增 NON_SERIOUS：**0**。
- OI-1 **CLOSED**；OI-2 **CLOSED**；OI-3 **SPEC_GAP_REQUIRES_VANGE_CONFIRMATION**（保持 NON_SERIOUS，不阻塞真机门）。

---

## 1. canonical 哈希独立复核（全部通过）

| 项 | canonical | 复核 |
|---|---|---|
| router.py | `35371BA2402EEFC035BC2B0B5CADBE0777402DE0849F9AC0FC27D8B6F68F3724` | ✓ 与 R1 一致 |
| pipeline.py | `E27684BD229D7E567C2F20FC45CD14377AC9CD2118CA052BB37ECF7AFAE967FE` | ✓ 与 R1 一致 |
| test_router.py | `E4288F3716653AFB2DA4F6F3B1BC5167880CF519B6D2FC755E9C2020656CC522` | ✓ 与 R1 一致 |
| test_pipeline.py | `9A54E673AFB6317E014AD9BAE02458714685C056B10603CE7C7CA4B438C66582` | ✓ 一致（仅测试变化） |
| 自测报告 | `CC82B0225D495C25898FC452E3D02DD263DF74D8C5DDDDB3025BD5102EEC5097` | ✓ 一致 |
| 完成报告 | `E553769EB8A8026860AA328150A579AF34A9BEBEB8780E43B7FC481DF1F58447` | ✓ 一致 |
| R1 报告 | `4DB0791B7CE2FBE4799FF9A9C4C98E94C28B358A6B7427072BD55DA6346B2773` | ✓ 一致（未改） |
| Git HEAD | `ff3ab942997b46a5beffb4daa76a66572adba7ef` | ✓ 未变 |

- 定向复跑：`tests/test_router.py tests/test_pipeline.py` → `32 passed in 1.78s`（与 R2 声明 32 passed 一致）。
- 全量复跑：`pytest tests -q` → `194 passed, 5 skipped, 1 warning in 80.69s`（与 R2 声明一致）。
- 唯一 warning 仍为既有 Starlette/httpx 弃用提示，本任务未引入。

---

## 2. OI-1 / OI-2 闭合核验

### OI-1 — CLOSED ✓

- 证据：`test_pipeline.py:100` 在 `test_replay_reuses_last_reply_without_llm_or_rag` 中新增 `assert timing["comfort_sent"] is False`。
- 判定：REPLAY 用例现显式断言不排 Hermes comfort，覆盖 Spec S1「不排 HERMES comfort」。实现本已正确（`response_route="replay"≠HERMES`，pipeline.py:567 不触发），现补上回归守卫。最小改动、公共接缝、断言有效。

### OI-2 — CLOSED ✓

- 证据：`PartiallyFailingStreamTTS`（test_pipeline.py:349-358）实现 `stream_synthesize` 先 yield 一帧再 `raise TTSError`；`test_streaming_tts_partial_failure_preserves_previous_snapshot`（test_pipeline.py:361-377）证明该不完整流式轮次不覆盖旧快照。
- 判定：该测试**真正走到流式路径**（`hasattr(tts, "stream_synthesize")` 为真，`preconn_state` 因无 `open_preconnect` 为 None，走 `_worker` 的非预连接流式分支）。`_drain_stream` 捕获 `TTSError` 返回 `completed=False`（pipeline.py:473-496），`_worker` 置 `delivery_failed[0]=True`（pipeline.py:533-534），提交门 `not delivery_failed[0] and total_frames > 0`（pipeline.py:711）阻止覆盖；部分音频帧仍正常发出。若 `delivery_failed` 未被置位，后续 `重复一次` 会重播「不应提交的流式回答。」而非「稳定回答。」，断言即失败——故该测试对 `delivery_failed→不提交` 的覆盖是真实而非假阳性。

---

## 3. OI-3 技术边界裁决

**裁决：SPEC_GAP_REQUIRES_VANGE_CONFIRMATION（非代码缺陷，Codex 不得自行猜测）**

- 事实：pipeline.py:349-353 CONTEXT+DATA 分支，数据不新鲜时 `_build_context_data_reply()`（pipeline.py:197-204）返回 `CONTEXT_NO_SNAPSHOT_TEXT`；提交门（pipeline.py:711-717）对 `context_followup` 无额外条件，故该降级轮仍按「成功 CONTEXT」以 `last_route=DATA` 覆盖快照。
- 现有 Spec 存在未闭合的两条并置条款：
  - §2 写入规则 2：「成功 CONTEXT 轮 → 覆盖快照」——未定义「数据不新鲜的降级轮」是否算「成功」；
  - §3.3 DATA 行：「复用 `_build_health_reply` 重报最新健康数据（若新鲜），否则回 `CONTEXT_NO_SNAPSHOT_TEXT` 诚实降级」——只规定降级文本，未规定其提交语义。
- 边界判断：该降级轮既非 §2 规则 4 的「失败/取消」，也非明确的「成功」，是 Spec 未覆盖的语义空白。两个方向均需产品判断（是否让「我有点没跟上」成为可重播的「上一轮回复」），属 Vange 权限，不属 Codex 权限。Codex 选择不擅自改业务代码，符合「未经 Spec 确认不得擅自开发」与「Spec 未覆盖先停下来」的纪律，处理正确。

**是否阻塞真机门：不阻塞。**
- BOX-3 真机主矩阵（生日复述 + 慢路径指代追问 + 全链路首字 ≤1484.589ms）不经过「DATA 查询 → 健康数据过期（>300s data_stale）→ CONTEXT → REPLAY」这条窄链路。
- OI-3 保持 NON_SERIOUS，可先推进真机主矩阵，与 OI-3 裁决解耦。

**唯一下一动作（针对 OI-3）：由 Vange 二选一**
1. 保持当前降级轮覆盖（现状不动）——则 Spec §2 需补充一句「DATA 无新鲜值的 CONTEXT 降级轮仍按成功 CONTEXT 覆盖」以闭合歧义；
2. 降级轮不覆盖——则需改 Spec（§2 规则 2 增补例外：`final_text == CONTEXT_NO_SNAPSHOT_TEXT` 时不提交）+ pipeline.py 提交门加一行守卫 + 补对应测试。
- 无论选哪个，Spec 文本需先由 Vange 确认，Codex 不得在未确认前动手。

---

## 4. 报告诚实性复核（D）

- 自测报告 §1/§9、完成报告 §3/§5：均如实标注 OI-1/OI-2 已关闭、OI-3 待 Spec/Vange 裁决、R2 待收口、BOX-3 真机门 PENDING、ISSUE Open；未把本地门写成项目完成。
- 自测报告 §7 明确「真机全链路首字红线 1484.589ms 尚未执行，本地 Fake 证据不能替代真机门」；§8 哈希与磁盘一致。
- 结论：报告更新诚实、准确，真机门与 ISSUE Open 状态均保留。

---

## 5. 本轮全部问题（一次性）

- **SERIOUS：0**
- **NON_SERIOUS 新增：0**
- **仍开放：OI-3**（SPEC_GAP，非代码缺陷，Vange 裁决后关闭）

---

## 6. 越界写入检查

- 本轮仅允许写入本报告 `协同工作文档/审查报告/2026-09-01-跨路径上下文联动修复-实现与自测-审查-第2轮_hm.md`。
- 未修改 R1 报告、实现、测试、Spec、任务单、_cdx 报告、Open-Issues.md、配置、Git、固件或设备；未 commit/push/reset/clean；未烧录/重启/采样；未发起网络业务请求。

---

## 7. 当前状态与唯一下一动作

- 当前状态：`PASS_WITH_NONBLOCKING_OPEN_ISSUES`；OI-1/OI-2 CLOSED；OI-3 SPEC_GAP 待 Vange 裁决；本地门已满足；真机门未执行；ISSUE-0013 保持 Open。
- 唯一下一动作：Vange 裁决 OI-3（保持覆盖 / 降级不覆盖）并决定是否同步推进 BOX-3 真机主矩阵（生日复述 + 慢路径指代追问 + 全链路首字 ≤1484.589ms）；四段门全满前 ISSUE-0013 不关闭。

---

*Hermes（deepseek-v4-pro）· 2026-09-01 · 后缀 _hm 遵循 agent.md §5.1*
