# 审查报告 —— 首字延迟优化 + 路由智能 + 人设增强（第 1 轮）

- **审查方**：Hermes（deepseek-v4-pro）
- **日期**：2026-08-17
- **对象**：4 个主题 commit（仓库卫生 + 首字延迟优化 + 路由智能 + 人设与 LLM 增强）
- **结论**：**REVIEW_PASS** —— 4 个 commit 质量高，全部对齐方案正确落地，测试 33 passed + 7 skipped。

---

## 一、逐 commit 核对

### 0c24590 仓库卫生 ✅
ISSUE 库去重（0007/0008 归位 Close_Issue）+ 0009/0010/0011 登记 + 文档归位会话存档 + agent.md v1.5（zcode 角色 + 文件后缀）。去重正确，无重复残留。

### 65b6325 首字延迟优化（方向1+3）✅
| 落地项 | 审查 |
|---|---|
| vendored 预连接协议（复用 edge_tts 内部函数） | ✅ 正确，`_EdgePreconnect` 只做建 WSS + speech.config，发 ssml 推迟到首句 |
| 预连接失败回退 | ✅ `open_preconnect` 失败返回 None；`_drain_stream` 首段失败（emitted_any=False）回退用时建连重合成 |
| 慢路径不预连 | ✅ `route != HERMES` 才预连，符合 C3 实测（2s 空闲恶化）；降级慢路径时禁用预连接 |
| 首段阈值 800 samples（50ms） | ✅ 首段即发首帧，后续段 4800 samples（300ms）减少帧数 |
| trust_env=False | ✅ 不读环境代理变量，符合红线 |
| 预连接用完即关 | ✅ 单句用完 close，无连接泄漏 |
| 锁版本 + aiohttp/certifi 显式声明 | ✅ requirements 锁 7.2.8 |

### af74a5b 路由智能（ISSUE-0010）✅
- 会话级路由记忆：`_last_route` + TTL 600s（对齐 LLM 历史过期），上轮慢路径 + 本轮指代追问 → 延续慢路径 ✅
- 礼貌/结束语白名单（谢谢/再见/晚安等 → 快路径），避免「慢路径后说谢谢」被误判追问 ✅
- 能力询问识别（能不能/能否 + 帮我/请你等祈使词兜底区分命令），不触发工具、不发安抚语 ✅
- 指代追问判定：短句 ≤10 字 +（追问词 或 反向新话题信号），ASR 丢轻声词兜底 ✅

### f557e41 人设与 LLM 增强 ✅
- 小V 改名 + 情感化提示词 + 诚实兜底：SYSTEM_PROMPT 完整落地 ✅
- 心跳保温：240s 周期、daemon 线程、max_tokens=1、失败静默重试、stop_heartbeat 显式停止 ✅
- tool_seen 修复：httpx 直读 SSE 监听 `event: hermes.tool.progress`，正确（查证回复方案落地）✅

---

## 二、测试

`pytest tests/` → **33 passed + 7 skipped**（新增 test_preconnect.py 4 用例全过；7 skipped 为需真机硬件用例）。

---

## 三、NON_SERIOUS 建议（不阻塞，不触发返工）

1. **路由记忆无独立单元测试**：ISSUE-0010 是核心改动，但 `tests/` 无 test_router.py，「上轮慢路径 + 指代追问 → 延续慢路径」这条关键路径缺单测。建议补 test_router.py（3~5 用例：指代追问延续 / 礼貌语不延续 / 能力询问直答 / TTL 过期）。登记后续小项。
2. `main.py` 用 `@app.on_event("startup")`（FastAPI deprecated），建议后续迁移 lifespan（技术债，非阻断）。

---

## 四、结论

REVIEW_PASS。方向1+3（延迟优化）、方案1（心跳保温）、tool_seen 修复、ISSUE-0001 安抚语改 route=hermes、ISSUE-0010 路由记忆、人设小V 全部正确落地。测试通过，无 SERIOUS 问题。

建议 WorkBuddy 后续补 test_router.py（第 1 条），属收尾小项。

*Hermes（deepseek-v4-pro）· 2026-08-17*
