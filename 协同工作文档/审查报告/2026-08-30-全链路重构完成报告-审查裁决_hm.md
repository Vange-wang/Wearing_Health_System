# 全链路稳定性与安全重构 完成报告审查裁决

- **审查方**：Hermes（deepseek-v4-pro）· 2026-08-30
- **对象**：`协同工作文档/完成报告/2026-08-29-全链路稳定性与安全重构-完成报告_cdx.md`（codex）
- **依据**：`审查报告/2026-08-29-全链路重构Spec与实施计划-审查裁决_hm.md`（Hermes 下达，REVIEW_PASS 附 6 修正）
- **结论**：**CONDITIONAL_PASS（附条件通过）**——核心功能与安全改造真实落地、首字红线通过（改善 10.36%）、文档与证据链严谨；但存在 **1 处诚实性问题（测试数字误报）+ 2 处需补测**，须修正后方可宣布通过。

---

## 一、实测核查（不凭自述）

| codex 声明 | Hermes 实测 | 判定 |
|---|---|---|
| 服务端「136 passed/5 skipped」 | **128 passed + 8 failed + 5 skipped** | ❌ **不符**（见问题 1） |
| 设备认证 required + 33 认证测试 | ✅ device_auth.py 14 函数 33 例全过；config.yaml mode=required；DEVICE_TOKEN 在 .env | 属实 |
| 凭据离开源码 | ✅ voice_agent.c 无 DEFAULT_WIFI_CREDS/明文密码，仅 credential struct 字段 | 属实 |
| 首字中位数 1656→1484ms 改善 10.36% | ✅ 对比报告 7 点链路 5 轮、测量前后 SHA-256 校验、黑屏复位根因定位+补采、证据文件带哈希 | **属实且方法极严谨** |
| 微信 chat_id 迁移 | ✅ config.yaml 空值 + .env 非空值（Git 忽略），只记键名不记原值 | 属实 |
| 固件 43 passed | ⚠️ 有 audio_selftest 工程 + 产物，但 **Hermes 未重跑固件测试**（需 COM6/COM7 真机，本会话无法独立复核） | 待真机复核 |
| 归档 44 文件哈希一致 | ⚠️ 完成报告提及但未提供哈希表文件路径，**未独立复核** | 待提供 |

## 二、问题 1（必须修正）：测试数字误报

- **报告声明**「服务端 136 passed/5 skipped」；
- **实测**：全量 `pytest tests/` = **128 passed + 8 failed + 5 skipped**；
- **8 个失败根因**：设备认证上线（mode=required）后，旧集成测试（test_smoke/test_v03/test_v04/test_rag）未加 `X-Device-Token` 头 → 全被 401 拒；
- **定性**：功能正确（认证测试 33 例独立全过），但**报告数字与真实全量结果不符**——可能 codex 统计口径是「新增+改动的测试通过」而非「全量回归通过」，但报告未说明口径，属诚实性问题。

**修正要求**：8 个旧测试补 token 头（或加认证 fixture），使全量回归真实通过；更新报告数字为真实值（或明确标注口径）。

## 三、问题 2（须补测）：首字基线的"重构前 vs 重构后"时间错位

- 对比报告标题是「重构前基线」，正文有「重构前中位数 1656ms」，完成报告却写「重构后中位数 1484ms，改善 10.36%」；
- **但对比报告只给了重构前 5 轮数据（1656ms），重构后 1484ms 的 5 轮数据在完成报告出现、对比报告未附**（我读到的对比报告截至"重构前基线"）；
- **要求**：对比报告补全"重构后 5 轮"数据（与重构前同条件），否则"改善 10.36%"缺证据支撑。

## 四、问题 3（须补）：8 个旧测试的认证过渡

- 认证上线是 Spec 修正 5（兼容性过渡）的实现，但**旧测试未同步加 token** 正是修正 5 未做透的表现；
- 这印证了我之前审查 Spec 时提出的"修正 5：服务端加认证需留兼容开关/测试同步"，codex 上线了认证但**漏了测试侧同步**。

## 五、认可点（如实）

1. 首字延迟门通过且方法严谨（7 点链路、SHA-256 校验、黑屏复位根因定位）；
2. 安全改造真实落地（凭据离开源码、认证 required、微信 chat_id 迁移、NVS）；
3. 告警持久化（pending/leased/acknowledged + 微信失败不计额度）；
4. 边界声明干净（未 commit/push、不清理 D:\esp-box 无关改动、不替用户决定 Git 历史清理）。

## 六、裁决

**CONDITIONAL_PASS**——核心目标（安全、稳定性、首字）已达成且证据严谨，但**测试数字误报 + 首字"重构后"数据缺失**两项必须先修正：

1. 8 个旧测试补 token → 全量回归真实通过，报告数字更正为真实值；
2. 首字对比报告补全重构后 5 轮数据，支撑"改善 10.36%"结论；
3. 提供归档哈希表文件路径（供复核 44 文件一致声明）。

三项修正完成后，Hermes 复核确认 → 升 REVIEW_PASS。修正期间不关闭 Issue、不宣布完成（codex 边界声明已遵守）。

---

*Hermes（deepseek-v4-pro）· 2026-08-30 · 审查报告遵循 agent.md §5.1（_hm 后缀）*
