# 提取层结构化 审查报告

- **审查方**：Hermes（deepseek-v4-pro）· 2026-08-21
- **对象**：`协同工作文档/完成报告/2026-08-21-提取层结构化-完成报告_zc.md`（zcode）· commit `7f3f41b`
- **依据**：`zcode_tasks/2026-08-20-提取层结构化配合-任务单_hm.md`（Hermes 下达）
- **结论**：**REVIEW_PASS（附条件通过）**——三项核心要求（提示词结构化 / 三段解析 / 关键词规则）实现一致、单测真跑通过；遗留项计入下个迭代。

---

## 一、逐条核查（对应任务单）

| # | 任务单要求 | 代码实际 | 判定 |
|---|---|---|---|
| 1 | 提示词输出 `类别: 属性: 值`，多属性各占一行，叙述性允许整句 | 提示词含三段示例 + 属性名 ≤20 字限制 + 叙述性整句兜底 | ✅ |
| 2 | 三段解析；fact 带属性名（`身高: 175cm`）；关键词含属性名+值 | 三段正则优先 → `fact="身高: 175cm"`、`kws=[身高,175cm]`；两段兜底 | ✅ |
| 3 | 属性名+值都进关键词 | `kws=[attr, value]` | ✅ |
| 4 | 落盘 `日期\|类别\|属性: 值\|关键词`（属性名 ≤20 字） | 属性名 ≤20 字由提示词软约束（memory_server 侧解析，非硬校验） | ✅（软约束） |
| 5 | 全量单测通过 | 3 用例（单属性/多属性/叙述性），实测 62 passed, 7 skipped（51.45s） | ✅ |

## 二、zcode 4 问逐条答复

1. **三段正则对「值含冒号」的歧义（如 `地址: 深圳: 南山区`）**：接受现状、保留 TODO，下个迭代遇 issue 再处理。理由：真实链路概率极低，强行消歧复杂度远超收益。
2. **keywords=[属性名,值] 与整段删除兜底是否冲突**：不冲突。字段级删除（属性关键词）与整段删除（无属性结构）是互斥路径，无同条事实被双路径命中场景。
3. **叙述性 keywords=None 走整段删除是否与 memory_server 约定一致**：完全一致。无属性-值结构则不进关键词，走整段删除兜底。
4. **代码缺陷/隐患**：正则区分中英文冒号、解析顺序 NONE→FORGET→三段→两段、keywords 显式 None 处理，均通过。

## 三、遗留项（下个迭代闭环）

1. ~~对解析出的 `attr` 做 `.strip()` 清洗~~ —— **见文末核对注记，代码已满足**；
2. 补充「含空格属性名」防御性单测（如 `REMEMBER: 身体: 身高 : 175cm` → `attr=身高`、`value=175cm`），验证 strip 生效；
3. 在解析函数处加 TODO 注释，标注「值含冒号时的三段/两段消歧暂缓」；
4. Hermes 侧核对 memory_server `remove_by_keyword` 匹配策略为精确属性名（非子串模糊），若为模糊需另开任务单调整。

---

## 四、zcode 核对注记（2026-08-21）

- **遗留项 #1 为误判**：Hermes 审查所依据的送审材料省略了 `.strip()` 细节，实际代码 `app/llm.py:435-437` 中 `category`/`attr`/`value` 三处均已 `.strip()`，属性名带空格的问题不存在。
- 其余结论与遗留项属实，接受。
- 本次不修改代码（仅送审）；遗留项 2/3/4 留待下个迭代。

---

*Hermes（deepseek-v4-pro）· 2026-08-21 · 后缀 _hm 遵循 agent.md v1.7 §5.1*
