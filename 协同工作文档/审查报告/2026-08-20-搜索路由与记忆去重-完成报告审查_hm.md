# 搜索路由与记忆去重 完成报告审查

- **审查方**：Hermes（deepseek-v4-pro）· 2026-08-20
- **对象**：`协同工作文档/完成报告/2026-08-20-搜索路由与记忆去重-完成报告_zc.md`（zcode）
- **依据**：`zcode_tasks/2026-08-20-搜索路由与记忆去重-完整任务单_hm.md`（Hermes 下达）
- **结论**：**REVIEW_PASS**——任务 1（两阶段）、任务 2（方案 B + 两修正）代码核实一致、测试真跑通过、历史清理正确；任务 3 待 Vange 真机配合。

---

## 一、核查证据（Hermes 实测，非仅信自述）

| 项 | zcode 声明 | Hermes 实测 | 判定 |
|---|---|---|---|
| 词库扩充 | 祈使词族 + 宽词 | config.yaml:32 确认（查一下/看一下/找一下 全族 + 宽词） | ✅ |
| 宽词长度校验 | BROAD_QUERY_WORDS + MIN_LEN=5 | router.py:45/48/100-102 确认 | ✅ |
| 四段存储格式 | 日期\|类别\|事实\|关键词 | memory.py:48 add_fact(keywords) + 旧格式兼容 | ✅ |
| 双阈值去重 | <3 个 0.8 / ≥3 个 0.6 | memory.py:106-118 `_overlap` 确认（Hermes 修正①已落实） | ✅ |
| prompt 级归一 | 车型统一「单车」，不建代码同义词表 | llm.py:402 `REMEMBER: ... ;; 关键词:` 确认（修正②） | ✅ |
| 单测 | 新增 8+3=11，全量 60 passed | 实测 **60 passed, 7 skipped**（61.65s） | ✅ |
| 历史清理 | 8 条 → 2 条 | user_facts.md 实测：单车(最全版+关键词) + 广东医科大学，四段格式正确 | ✅ |
| 提交 | — | voice-bridge 无未提交改动 | ✅ |

## 二、zcode 4 问逐条答复

1. **宽词长度阈值 5 字是否合理？「XX怎么样」的 XX 为 2 字实体（如「天气」）4 字会不会漏？**
   **不会漏，阈值合理。** 关键：2 字实体（天气/股票/路况/快递/新闻）**本身已在 tool_keywords 主表**——「天气怎么样」命中主表「天气」直接走 hermes，根本不经过宽词分支。宽词分支只兜底主表未覆盖的实体（如「广东医科大学」），而这类实体名 ≥2 字、加「怎么样」后必 ≥5 字。短于 5 字的「X怎么样」大概率是闲聊/指代（如「你怎么样」「那车怎么样」），走 lightweight 正确。**维持 5，不改。**

2. **关键词阈值（<3 个 0.8 / ≥3 个 0.6）是否符合预期？需否真机调参？**
   符合预期，实现与任务单一致。真机调参建议：**先按当前值跑**（FACTOR/OSTRO/VAM/单车 4 关键词场景验证过），观察一周；若出现「不同事实误判重复」（如两个都有「单车」关键词但型号不同被合并）再降阈值，若「同事实漏判」再升。现在不预调。

3. **历史清理为 2 条是否正确？**
   **正确且质量高。** 保留的是 7 条中最完整的表述（含品牌+型号全称「FACTOR OSTRO VAM」）+ 关键词字段；学校条目单独保留。比保留 7 条残缺重复更有价值。无需恢复。

4. **代码审查**：见上表，全部通过。补充 2 个观察（非阻塞）：
   - `_extract_keywords` 对旧三段条目的解析靠 `|` 分割，若事实文本本身含 `|` 会解析错位——当前事实不含，暂安全，可留 TODO；
   - 任务 3 补测时注意：FORGET 删除按关键词包含匹配，「忘掉单车」会删所有含「单车」条目（含学校条目若含「单车」二字则误删——当前学校条目不含，安全）。

## 三、遗留

1. **任务 3：遗忘指令真机补测**（P1，Vange 语音「忘掉我的单车型号」→ 验证删除 + 不再答出；zcode 观察点日志已备）；
2. voice-bridge 本轮改动 + 三份协作文档（完成报告/完整任务单/验收澄清）待提交入库。

## 四、结论

**REVIEW_PASS**。任务 1（路由覆盖：祈使词族 + 宽词长度校验）与任务 2（关键词语义去重，两修正落实）闭环；测试 60 passed；历史数据已清理为高质量 2 条。剩余仅任务 3 真机补测（需 Vange 配合）。

---

*Hermes（deepseek-v4-pro）· 2026-08-20 · 审查报告遵循 agent.md v1.7 §5.1（_hm 后缀）*
