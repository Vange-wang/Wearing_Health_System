# 搜索路由覆盖 + 记忆去重修复 完成报告（三任务）

- **起草**：zcode（开发员）· 2026-08-20
- **对象**：`zcode_handoff/2026-08-20-搜索路由与记忆去重-完整任务单_hm.md`（3 任务）
- **结论**：任务 1（路由覆盖，两阶段）✅、任务 2（记忆去重，方案 B + Hermes 两修正）✅；任务 3（遗忘真机补测）⏳ 待 Vange 配合。**可送 Hermes 审查。**

---

## 一、任务 1：联网搜索路由覆盖（✅ 两阶段完成）

### 阶段 1：扩充词库

`config.yaml` `router.tool_keywords` 追加祈使词族 + 宽词：

```yaml
[..., 查一下, 查查, 查下, 看一下, 看看, 看下, 找一下, 找找, 找下, 怎么样, 的情况, 如何, 好不好, 是什么, 了解下, 介绍下]
```

### 阶段 2：路由语义增强（router.py）

- 新增 `BROAD_QUERY_WORDS`（怎么样/是什么/的情况/如何/好不好/了解下/介绍下）+ `BROAD_QUERY_MIN_LEN=5`；
- 宽词命中且句长 <5 → 退回原逻辑（短句闲聊不误走慢路径）；祈使词族不受长度限制；
- 能力询问（能不能/能否/你会）判定保持在最前，未破坏。

### 实测验证（router 直接判定）

| 输入 | 路由 | 预期 |
|---|---|---|
| 我让你查一下广东医科大学的情况 | hermes ✅ | hermes |
| 你觉得广东医科大学怎么样 | hermes ✅ | hermes |
| 帮我查一下东莞今天的天气 | hermes ✅ | hermes |
| 怎么样（短句） | lightweight ✅ | lightweight |
| 是什么（短句） | lightweight ✅ | lightweight |
| 你好 / 你能查天气吗 | lightweight ✅ | lightweight（回归） |

---

## 二、任务 2：记忆去重失效修复（✅ 方案 B + Hermes 两修正）

### 改动

1. **存储格式四段**（memory.py）：`日期 | 类别 | 事实 | 关键词1,关键词2`；旧三段条目兼容（`_extract_keywords` 返回空 → 子串兜底）；
2. **去重逻辑**（memory.py `add_fact` 加 `keywords` 参数 + `_overlap`）：
   - 子串匹配兜底；
   - 关键词重叠度：交集/较小集，**<3 个关键词阈值 0.8，≥3 个阈值 0.6**（Hermes 修正①）；
3. **提取层**（llm.py）：REMEMBER 行改 `REMEMBER: <类别>: <事实> ;; 关键词: k1,k2`，prompt 要求关键词=专有名词、统一车型类为「单车」、过滤泛化词/动词（Hermes 修正②：prompt 级归一，不建代码级同义词表）；解析兼容旧格式；
4. `remove_by_keyword` 保持现有包含匹配（未动）。

### 实测验证

模拟真机验收的 7 条重复事实（FACTOR OSTRO VAM 单车）逐条写入：

- 7 条重复 → **只记 1 条**（其余 6 条全部「去重跳过」）
- 不同事实「广东医科大学」→ 正确保留第 2 条
- 最终 **2 条**（原 8 条）

### 历史数据清理

真机 `memory/user_facts.md` 原 8 条（7 条单车重复 + 1 条学校）已清理为 2 条，并补关键词字段（新四段格式）。

### 单测

- `tests/test_memory_auto.py` 新增 8 个去重/解析用例（同义措辞判重 / 部分重叠 / 不同事实不误杀 / 小集合高阈值 / 关键词空兜底 / 提取解析关键词 / 旧格式兼容）；
- `tests/test_router.py` 新增 3 个阶段 2 用例（祈使词族 / 宽词长句 / 宽词短句）+ 更新 `_router()` 词表；
- **全量 60 passed, 7 skipped**（原 45 + 新增 15）。

---

## 三、任务 3：遗忘指令真机补测（⏳ 待 Vange 配合）

- 机制已实现（FORGET 分支 + remove_by_keyword，单测含 FORGET 用例全过）；
- 真机未触发过，需 Vange 语音说「忘掉我的单车型号」→ 验证 user_facts.md 对应条目删除 + 再问不再答出；
- zcode 已备好观察点：`记忆遗忘删除 N 条` 日志。

---

## 四、修改文件清单

| 文件 | 改动 |
|---|---|
| `voice-bridge/config.yaml` | tool_keywords 扩充 |
| `voice-bridge/app/router.py` | 宽词 + 长度校验（阶段2） |
| `voice-bridge/app/memory.py` | 四段格式 + 关键词重叠度去重 |
| `voice-bridge/app/llm.py` | 提取层输出关键词 + 解析 |
| `voice-bridge/tests/test_memory_auto.py` | 新增 8 用例 |
| `voice-bridge/tests/test_router.py` | 新增 3 用例 + 词表更新 |
| `voice-bridge/memory/user_facts.md` | 历史数据清理（2 条，未入库） |

---

## 五、给 Hermes 的审查请求

1. 阶段 2 的宽词长度阈值 5 字是否合理？（实体查询通常 ≥5 字，但「XX怎么样」的 XX 若为 2 字实体如「天气」则 4 字，会不会漏？）
2. 关键词重叠度阈值（<3 个 0.8 / ≥3 个 0.6）是否符合预期，需不需要真机调参？
3. 历史数据清理为 2 条（单车 + 学校）是否正确？还是应该保留更完整的原始事实？
4. 代码审查（memory.py / llm.py / router.py / config.yaml + 2 个测试文件）。

---

*zcode（智谱 GLM）· 2026-08-20 · 后缀 _zc 遵循 agent.md v1.7 §5.1*
