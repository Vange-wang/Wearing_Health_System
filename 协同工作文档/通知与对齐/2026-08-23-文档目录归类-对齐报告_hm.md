# 文档目录归类 对齐报告（2026-08-23）

- **执行方**：Hermes · 2026-08-23 · Vange 确认分类方案后执行
- **目的**：消除顶层文件堆积，按文档类型归类；同步更新全部引用路径
- **状态**：✅ 完成（移动 33 个文件，更新 35 个文件/37 处引用，残留检查通过）

---

## 一、协同工作文档（新建 4 个目录）

| 新目录 | 内容 | 文件数 |
|---|---|---|
| `完成报告/` | 各阶段完成报告（zcode/Hermes 产出） | 9 |
| `验收与澄清/` | 验收报告、澄清说明、问题分析 | 4 |
| `通知与对齐/` | 指令报告、角色变更通知、对接回复、方案对齐 | 5 |
| `清单与裁决/` | 待办清单、Spec 修订裁决清单、旧任务单 | 3 |

**保留原位**：`审查报告/`、`ISSUE/`、`zcode_tasks/`、`zcode_handoff/`、`Hermes_handoff/`、`WB_handoff/`、`会话存档/`、`README.md`

## 二、Code文档（新建 2 个目录）

| 新目录 | 内容 | 文件数 |
|---|---|---|
| `自测报告/` | 各版本自测报告（v0.1~v0.4/长期RAG/专项）+ v0.1自测报告-目录 | 8 |
| `技术笔记/` | ESP32 编译烧录配方、ESP-IDF 安装记录、voice-bridge 自助排查手册 | 3 |

**保留原位**：`固件/`、`v0.1-环境验证/`、简历项目经历、`README.md`

## 三、引用更新（全库扫描）

- **更新**：35 个文件、37 处引用（审查报告、交接文档、agent.md、Spec、里程碑、任务单、README 等）→ 全部指向新路径；
- **跳过**（历史快照，不改）：`.workbuddy/memory/`、`.tmp-sync/`、`会话存档/`——属归档性质，保留当时上下文；
- **验证**：活动文档 0 处残留旧路径（脚本全库扫描确认）。

## 四、后续规范（约定）

1. **新文档直接入对应目录**（完成报告→`完成报告/`、自测报告→`自测报告/`、审查→`审查报告/` 等），不再放顶层；
2. 任务单→`zcode_tasks/`、交接→`zcode_handoff/`（沿用既有约定）；
3. 引用路径更新：本次已全量修复；后续移动文件必须同步扫描引用（可复用本次脚本逻辑）。

## 五、git 状态

- 85 个变更（移动 = 删除 + 新增 + 引用修改），**未提交**——Vange 确认后提交。

---

*Hermes（deepseek-v4-pro）· 2026-08-23 · 后缀 _hm 遵循 agent.md §5.1*
