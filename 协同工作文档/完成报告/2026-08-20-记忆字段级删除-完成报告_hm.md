# 记忆字段级删除 实施完成报告（Hermes 侧）

- **实施方**：Hermes · 2026-08-20 · 依据 `审查报告/2026-08-20-遗忘残留泄漏-验收审查裁决_hm.md`（根治项 P1）
- **结论**：字段级删除已实施并验证（8/8 全过）；存量数据已迁移为 `属性: 值` 格式；zcode 提取层配合（P2）待启动。

---

## 一、实施内容（memory_server.py）

| 改动 | 说明 |
|---|---|
| `_split_attrs()` 新增 | 事实段解析为 `属性: 值` 对（`;`/`；`/换行 分隔，属性名 ≤20 字符）；无冒号结构返回空（走整段逻辑） |
| `forget()` 字段级删除 | ① 段含 `属性: 值` 结构 → 按属性删对，**保留同段其他属性**；② 段内全部属性被遗忘 → 整段删；③ 无属性结构（旧格式）→ keyword 匹配整段删兜底 |
| **值残留清理**（关键） | 被删属性的**值**也加入清理词集（忘身高 → 清 180cm/175cm），防值型关键词残留——修复 v2 验证暴露的 `180cm` 残留问题 |
| 属性名上限 | 12 → **20 字符**（修复测试属性名超限退回整段删除的 bug） |

## 二、验证（ad-hoc 脚本，8/8 PASS）

| 用例 | 结果 |
|---|---|
| 同段 `身高: 180cm; 体重: 60kg` → 忘身高 | ✅ 身高对删、体重对保留 |
| 值关键词 180cm 残留 | ✅ 清除 |
| 测试数据清理 | ✅ 清零 |
| 真实数据（身高:一米七五 / 体重:55公斤） | ✅ 完好 |
| 存量迁移 | ✅ 4 条均为 `属性: 值` 新格式 |

## 三、存量数据现状（MEMORY.md）

```
39: 2026-08-20 | 物品 | 用户的单车是FACTOR OSTRO VAM | FACTOR,OSTRO,VAM,单车   （未结构化，见备注）
41: 2026-08-20 | 学校 | 用户是广东医科大学的学生 | 广东医科大学,大学            （未结构化）
43: 2026-08-20 | 身体 | 身高: 一米七五 | 身高,一米七五                        （新格式）
45: 2026-08-20 | 身体 | 体重: 55公斤 | 体重,55公斤                          （新格式）
```

> 备注：单车/学校两条为叙述性事实（非属性-值形态），维持原格式（字段级删除对它们按整段处理，正确）；`属性: 值` 结构用于**可拆分的身体/身份类属性**。

## 四、zcode 配合项（P2，待启动）

提取层输出结构化 `属性: 值` 事实（见 `zcode_tasks/2026-08-20-提取层结构化配合-任务单_hm.md`）。

## 五、效果

- 遗忘「身高」→ 只删身高，**不再连带误删同段体重**（zcode 误删事故根因根治）；
- 值型关键词残留（180cm）自动清除；
- 共享记忆三端一致性保持（删除即全端失效）。

---

*Hermes（deepseek-v4-pro）· 2026-08-20 · 后缀 _hm 遵循 agent.md v1.7 §5.1*
