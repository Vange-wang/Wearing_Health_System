# 全链路稳定性与安全重构完成报告

- 产出方：Codex（开发员）
- 日期：2026-08-30
- 当前状态：**REVIEW_PASS**
- 最终裁决：Hermes 已在 `协同工作文档/审查报告/2026-08-30-全链路重构完成报告-复核裁决_hm.md` 独立复核三项整改并升级为 `REVIEW_PASS`

## 1. 交付摘要

按已审查 Spec 与 19 项实施计划完成了工作源码重构、服务端可靠性与安全改造、COM5/COM6 真机联调、首字延迟门、文档更新和固件归档同步。

关键结果：

- 设备真实凭据离开源码，改用本地配置 + NVS；服务端最终认证为 `required`。
- 腕带生产候选为 P1 简单峰检测 + LED 0x17，稳定点与外部血氧仪差值 1 个百分点/1 bpm，BLE `lost=0`。
- BOX-3 长按语音完整、短按/双击无空请求、首帧即播放，BLE 与语音并行无卡顿。
- 告警改为持久化 pending/leased/acknowledged，失败可重投；微信失败不计额度且 iLink 子进程不继承代理。
- 首字主指标中位数 1656.169 ms → 1484.589 ms，改善 10.36%，首字红线通过。
- 服务端在认证配置 `required` 且不注入 `observe` 覆盖的条件下真实全量回归为 136 passed/5 skipped；固件 43 passed；两套生产构建退出码 0。
- 固件归档同步 44 个点名文件，另核对 4 个既有 emoji 资源，共形成 48 项源/归档 SHA-256 表，不一致数 0；密钥命中、禁入文件、零字节、冲突标记和必需模块缺失均为 0。哈希表位于完整路径 `D:\workbuddy_project\项目\可穿戴健康辅助系统\Code文档\自测报告\2026-08-29-全链路重构自测报告_cdx.md` 的“第 6 节：固件归档 SHA-256 对照”。

## 2. 文档交付

- `规划文档/技术验证/2026-08-29-首字延迟重构对比_cdx.md`：第 6 节已补重构后五轮逐轮数据，第 94～107 行给出 10.36% 计算、P95、波动判定和日志路径。
- `voice-bridge/README.md`：已补认证、raw PCM、告警 ack、模块边界和运行要求。
- `Code文档/固件/wrist_node/README.md`：已补 P1+4.6mA 选择、真机结果和残余微动风险。
- `Code文档/固件/box3_voice_agent/README.md`：已补 NVS、认证、AudioSession/WavStream、构建烧录和联调结果。
- `规划文档/Hermes语音开发板实现方案-20260811.docx`：已更新 Edge-only TTS、raw PCM、ESP-IDF 5.2.7、构建路径、认证、模块边界和真机结果；SHA-256 `19D0B8C5E8965B391C674567599307944A719D4C7FD23EE975C9EEAE1F3174BC`。已渲染 8 页并逐页检查，无截断、重叠、乱码或表格破裂。
- `Code文档/自测报告/2026-08-29-全链路重构自测报告_cdx.md`：完整命令、退出码、真机、E0/E1/E2、延迟、归档哈希和残余风险。
- `Code文档/自测报告/2026-08-29-微信接收标识迁移与暴露范围_cdx.md`：只含键名、存在性、影响提交数和轮换/授权边界，不含原值。

## 3. 请求 Hermes 审核的重点

1. 审核 P1 + LED 0x17 的生产选择是否接受当前有限稳定/动作/重新放置证据，以及是否要求追加强灌注微动专项测试。
2. 审核设备认证、raw PCM 输入限制、会话锁空闲超时、告警租约/ack 和微信重试状态机是否满足 Spec。
3. 审核 BOX-3 内部 RAM/PSRAM 分配与 TALK 任务 1628 bytes 高水位余量是否接受。
4. 复核首字中位数、波动判定和“不触发 10 轮 P95”的结论。
5. 复核归档 48 项哈希表、密钥扫描和文件边界。
6. 裁决根目录人工 `test_e2e_sim.py` 的 pytest 误收集是否另开小修；本轮未越过 File Map。

## 4. CONDITIONAL_PASS 整改回执

1. **旧测试认证过渡**：已先复现 `8 failed、128 passed、5 skipped`，再为四份旧测试的公开 HTTP 请求补应用实际令牌；`required` 模式下无兼容覆盖复跑为 `136 passed、5 skipped、1 warning`，退出码 0。详细红绿证据见自测报告第 2.1 节。
2. **重构后首字五轮**：证据已存在于完整路径 `D:\workbuddy_project\项目\可穿戴健康辅助系统\规划文档\技术验证\2026-08-29-首字延迟重构对比_cdx.md` 第 6 节；逐轮主指标为 1484.589、1381.342、1536.840、2000.960、1418.771 ms，中位数 1484.589 ms。
3. **归档哈希表路径**：完整路径 `D:\workbuddy_project\项目\可穿戴健康辅助系统\Code文档\自测报告\2026-08-29-全链路重构自测报告_cdx.md` 第 6 节，共 48 项（44 个同步文件 + 4 个既有 emoji），源/归档/表内哈希不一致数 0。

以上三项对应 `协同工作文档/审查报告/2026-08-30-全链路重构完成报告-审查裁决_hm.md` 第六节要求，并作为随后 Hermes 复核的输入。

Hermes 已于 2026-08-30 完成复核，实测服务端 `136 passed, 5 skipped, 1 warning`，并确认首字后测五轮与48项归档哈希表，最终裁决为 `REVIEW_PASS`。

## 5. 残余风险

- P1+4.6mA 首次组合验证，强灌注切迹、不同手指/压力和长期佩戴仍可能暴露微动假跳。
- COM6 的物理光路/接触衰减曾出现，当前放稳可恢复，但软件不能永久消除模块与接触差异。
- E0 COM6 独立原始串口文件未冻结；COM7 回滚记录可证明参考基线，但证据链并非完整实验室数据集。
- TestClient/httpx2 deprecation warning 和人工 E2E 脚本误收集未在本 Spec 内修复。
- 当前设备不是医疗器械；不得把有限对照扩展为医疗诊断精度声明。

## 6. 边界声明

- 实施与审查阶段未执行 Git commit、push、merge、reset、历史重写或远端发布；Hermes `REVIEW_PASS` 后，Vange 于 2026-08-30 单独批准两个本地仓库按白名单提交，明确不推送。
- 固件工作源码已在 `D:\esp-box` 形成白名单本地提交 `4025b229e05fbd27fb4b53e287bbe3648ab0a527`；本项目仓库由包含本报告的随后本地提交收口。
- D:\esp-box 中大量既有无关修改和生成物保持原样，未清理、未覆盖。
- 微信接收标识的平台侧轮换、Git 历史清理和远端强制推送均需用户另行授权。
- `REVIEW_PASS` 只裁决本次全链路重构；不自动关闭 Issue，也不授权远端推送、历史重写或平台侧操作。
