# 2026-09-01 跨路径上下文联动修复完成报告

## 当前结论

- Codex 白名单实现与本地自测已完成，结果 **PASS**。
- Hermes R3 已 `PASS`（SERIOUS 0 / NON_SERIOUS 0，OI-1/OI-2/OI-3 全部 CLOSED）；BOX-3 真机行为与设备侧首字门也已通过。
- 当前状态：`DEV_SELFTEST_PASS / HERMES_R3_REVIEW_PASS / HARDWARE_ACCEPTANCE_PASS / HERMES_FINAL_ISSUE_CLOSURE_PENDING / ISSUE_OPEN`。

## 修改了什么

1. `voice-bridge/app/router.py`
   - 新增 8 个 REPLAY、6 个 CONTEXT 完整复合短语。
   - 新增公共分类接缝 `Router.classify_followup()`。
   - 普通词“一次/一遍/刚才/那个”等不单独触发；ISSUE-0010 原追问路由逻辑保持不变。
2. `voice-bridge/app/pipeline.py`
   - 新增单轮、内存-only、600 秒 TTL 的成功轮快照，字段为 user/reply/route/ts。
   - 锁只保护 O(1) 字典复制/替换，不持锁做 LLM、RAG、TTS 或网络 I/O。
   - REPLAY 直接把上一轮回复送入分句/TTS，零 LLM、零 RAG、无 Hermes comfort；无快照时返回固定本地模板。
   - CONTEXT 按上一轮 LIGHTWEIGHT/HERMES/RAG/DATA 分别闭合；固定前缀只带上一轮，reply 上限 500 字。
   - RAG CONTEXT 不二次检索；DATA CONTEXT 不调用 LLM，只重报最新新鲜健康数据。
   - 普通轮和 CONTEXT 只在全部帧成功交付后提交快照；REPLAY、TTS/LLM 失败和取消均不写快照。
3. `voice-bridge/tests/test_router.py`
   - 新增 REPLAY/CONTEXT 正例和 6 个误判负例。
4. `voice-bridge/tests/test_pipeline.py`
   - 新增 REPLAY、四类 CONTEXT、诚实降级、TTL、覆盖、不刷新、失败、取消、降级和并发测试。
   - 按 Hermes R1 OI-1/OI-2 补充 `comfort_sent=False` 断言和流式 TTS 中途失败不覆盖快照测试。
   - 按 Vange 的 OI-3 裁决补充完整链路：DATA 旧值失效后 CONTEXT 诚实降级覆盖，随后 REPLAY 只重播降级文本、不重播旧心率/血氧。

## 没有修改什么

- 未修改 `llm.py`、TTS、ASR、VAD、splitter、health、配置、依赖或 API。
- 未修改 ESP32/MAX30102/BLE 8 字节帧、SpO2 有效性门或告警质量门。
- 未烧录、重启、采样、提交、推送、reset 或 clean。
- 既有 26 个非白名单未跟踪文件 SHA-256 全部未变。

## 验证摘要

- 修改前全量：`172 passed, 5 skipped, 1 warning`。
- 修改后定向：`74 passed, 1 warning`。
- R1 补测后定向：`32 passed`；全量：`194 passed, 5 skipped, 1 warning`；无新增 fail/skip/warning。
- OI-3 补测后定向：`33 passed`；正常权限全量：`195 passed, 5 skipped, 1 warning in 85.49s`；无新增 fail/skip/warning。
- BOX-3 行为：生日→REPLAY 再次回答相同生日；天气 Hermes 搜索→“刚才那个呢”继续回答天气；无答非所问。
- BOX-3 固定“你好”有效 N=10：设备侧松键→codec 首 PCM 中位数 `1391.754ms`，冻结红线 `1484.589ms`，余量 `92.835ms`；P95 `2037.571ms`，最大值 `2171.783ms`。
- 全部 14 个采样（含 3 个 ASR 无效/截断和 1 个有效冗余样本）均保留于真机验收报告及原始证据目录，没有选择性丢弃长尾或无效样本。
- 本地 N=10 REPLAY 首帧：median `0.260ms`，p95 `0.318ms`。
- 同文本普通 LIGHTWEIGHT：median `0.428ms`，p95 `0.464ms`。
- REPLAY 额外 LLM 调用：`0`；满足本地 `+50ms` 门。
- 详细 TDD、哈希和时延数据见 `Code文档/自测报告/2026-09-01-跨路径上下文联动修复自测报告_cdx.md`。

## 交付边界与下一动作

- Hermes R3 verdict 为 `PASS`，SERIOUS 0 / NON_SERIOUS 0；OI-1/OI-2/OI-3 全部关闭。BOX-3 真机语音与首字证据已落入 `协同工作文档/验收与澄清/2026-09-01-ISSUE-0013-BOX3真机语音与首字验收_cdx.md`。
- 当前尚不能由 Codex 宣布 ISSUE-0013 Closed；最终 Issue 状态变更属于 Hermes 验收裁决。
- 唯一下一动作：把真机报告与原始证据交同一 Hermes session `20260901_004431_11dfd7` 核验；通过后由 Hermes 将 ISSUE-0013 从 Open 移入 Close，并记录关闭依据。
