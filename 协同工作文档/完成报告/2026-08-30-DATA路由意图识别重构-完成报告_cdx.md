# DATA 路由意图识别重构完成报告

- 产出方：Codex（开发员）
- 执行时间：2026-08-30 至 2026-08-31（Asia/Shanghai）
- 当前状态：**READY_FOR_HERMES_REVIEW**
- 审核状态：等待 Hermes 独立复核；Codex 不自行宣布 `REVIEW_PASS`

## 1. 交付摘要

已按 Hermes 任务单和实施计划完成 DATA 路由意图识别重构：

- 只有明确查询当前健康数值的短语进入 DATA/template。
- 正常范围、原因、关系、异常判断和选购问题进入 RAG/lightweight，不再被健康字段子串截胡。
- lightweight 可只读引用 60 秒内的新鲜心率/血氧；字段缺失或过期时禁止编造。
- 普通“你好”不读取健康数据、不增加健康提示词 token；公共提示词与改动前 HEAD 完全一致。
- 修复真机发现的两项回归：`雪阳` 近音归一，以及 DATA 后“你好”错误继承 DATA。
- 完成 Hermes `CONDITIONAL_PASS` 唯一 SERIOUS：单问心率只报心率、单问血氧只报血氧，双字段才双报。
- 正式测试为 172 passed、5 skipped、1 warning；真机确定性 DATA、语义回答、过期保护和首字门已执行。

## 2. 首字与真机结论

- 普通 `你好` 修正后 10 轮首字中位数 **1427 ms**，冻结基线 **1484.589 ms**，主门禁通过；插值 P95 2048.7 ms，最大值 2199 ms，外部 LLM/TTS长尾保留给 Hermes 复核。
- 确定性 `我的心率多少` 5 轮首字中位数 **1460 ms**，改动前同路径 **1469 ms**，全部为 DATA/template，没有回退。
- 新鲜数据语义问答可引用腕带实测值；超过 60 秒后，lightweight 和 DATA 均不复述旧值。
- 血压当前值明确答未接入；未进入 Hermes 慢路径。

Hermes S1 整改后的三条真机证据：

- `我的心率多少` → `您当前心率 63，正常。`，未出现血氧，首字 1398 ms。
- `血氧是多少` → `您当前血氧 99。`，未出现心率，首字 1334 ms。
- `心率跟血氧` → `您当前心率 66，正常，血氧 99。`，首字 1293 ms。

三条均为 DATA/template，ASR 完整准确。整改冻结日志：`C:\Users\86166\AppData\Local\Temp\data-routing-20260831-cdx\voice-bridge-single-field-remediation.log`，SHA-256 `DA0FD674214DC557707BB52443D5DA9288C382F98370A1C4271D6821E5982FE2`。

完整逐轮数据、红绿测试、路由矩阵、日志哈希和排除样本见：

`D:\workbuddy_project\项目\可穿戴健康辅助系统\Code文档\自测报告\2026-08-30-DATA路由意图识别重构自测报告_cdx.md`

冻结日志：

`C:\Users\86166\AppData\Local\Temp\data-routing-20260831-cdx\voice-bridge-live-final.log`

SHA-256：`D2F334BBE3EA05375FEE57A7AF9B002270475703A13F8E4AE10BA41D6D4502EC`

## 3. 请求 Hermes 重点复核

1. 窄 DATA 短语、知识排除、RAG 优先和健康领域 lightweight 兜底的路由顺序是否符合任务单。
2. 健康上下文逐字段新鲜度、过期三态和“普通请求零 provider/零健康 token 增量”是否满足首字红线。
3. 172 项正式测试、真机 DATA/语义/过期矩阵和两个 TDD 现场回归修复是否充分。
4. 普通通道 10 轮中位数通过但 P95/极差仍有外部波动，是否允许作为残余风险进入提交门。
5. 复核 `CONDITIONAL_PASS` 唯一 SERIOUS 已闭环：字段选择逻辑、红绿测试、正式全量和三条真机证据是否足以升级 `REVIEW_PASS`。
6. “血氧仪怎么选”在 BOX-3 上多次整句 ASR 错识别，以及“血氧一般会同步变化”的健康知识表述风险，应在本任务整改还是另立 ASR/内容质量任务。

## 4. 已知残余风险

- 手指微动/接触漂移时出现心率 104、111、116；重新放稳后恢复。该风险来自传感器/算法现场条件，不是路由生成的数值。
- `雪阳` 是可安全归一的稳定近音；“血氧仪”被识别成“血氧乙/去转移”等整句错误，不宜继续用无限替换表掩盖。
- 普通通道存在外部 DeepSeek/Edge TTS长尾；中位数未回退，但最大值高于冻结五轮最大值。
- 健康知识回复尚非经医学内容审核；系统不构成医疗诊断设备。

## 5. 边界声明

- 本任务未修改、构建或烧录 `D:\esp-box` 固件。
- 未新增依赖、模型、HTTP 调用、代理、线程或定时器；DeepSeek 保持 `trust_env=False` 直连。
- 未执行 Git commit、push、merge、reset、历史重写或清理脏工作树。
- 当前 Git 暂存区为空；无关并行任务文档不在本任务交付白名单中。
- 只有 Hermes 给出 `REVIEW_PASS` 后，才向 Vange 申请新的精确提交/推送授权；此前保持工作树未提交状态。

## 6. CONDITIONAL_PASS 整改回执

Hermes 于 `协同工作文档/审查报告/2026-08-31-DATA路由意图识别重构完成报告-审查裁决_hm.md` 给出 `CONDITIONAL_PASS`，唯一 SERIOUS 为单字段回复粒度。

整改过程：

1. 先在 `test_data_routing_intent.py` 增加字段粒度断言，原实现稳定失败：心率单问仍包含血氧。
2. 最小修改 `StreamingPipeline._build_health_reply(text)`：按“心率/心跳”和“血氧”字段选择输出；无显式字段的 DATA 追问保持既有兼容；血压口径不变。
3. 最小用例 1 passed；DATA/健康相关套件 61 passed；正式套件 172 passed、5 skipped、1 warning。
4. 重启 required 服务并完成三条真机复测，单字段与双字段行为均符合裁决，首字 1293–1398 ms。

当前重新提交 Hermes 复核，状态仍为 **READY_FOR_HERMES_REVIEW**，不自行升级结论。
