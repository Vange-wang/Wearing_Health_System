# codex 任务单：跨路径上下文联动修复（ISSUE-0013）

- **下达方**：Hermes（deepseek-v4-pro）· 2026-09-01 · 后缀 `_hm`
- **状态**：SPEC_CLARIFICATION_CONFIRMED / IMPLEMENTATION_R3_REVIEW_PENDING——Vange 已确认 OI-3 语义（DATA 无新鲜值的 CONTEXT 诚实降级轮仍按成功 CONTEXT 覆盖快照并刷新 ts），不再进入 USER_CONFIRMATION_PENDING；业务代码无需修改，仅 Codex 补对应测试/报告后交 R3
- **依据**：Spec `规划文档/Spec文档/2026-09-01-跨路径上下文联动修复-spec_hm.md`（已吸收 S1-S7 + OI-3 澄清：DATA 无新鲜值 CONTEXT 诚实降级轮按成功 CONTEXT 覆盖）
- **性质**：voice-bridge 路由 + pipeline 修复，严格单线程，无真机依赖（真机验收是后续独立门）

---

## 1. 目标

修复 ISSUE-0013「重新说一遍丢上下文」：慢路径（HERMES）之后的指代追问能正确重播/延续，不答非所问；首字延迟零回退（真机全链路首字红线 1484.589ms 保持）。

## 2. 允许 / 禁止写入

### 允许（仅这些文件）

1. `voice-bridge/app/router.py`（§3.1 phrase-level 分类：REPLAY_PHRASES / CONTEXT_PHRASES + classify_followup，含负例保障）
2. `voice-bridge/app/pipeline.py`（§3.2 会话快照 + REPLAY 执行模式 + CONTEXT 上下文 + 诚实降级）
3. `voice-bridge/tests/test_router.py`（新增 phrase 分类用例，含 S4 负例）
4. `voice-bridge/tests/test_pipeline.py`（R1/R2 已精确新建，现为 File Map 内既有文件；R3 仅允许**追加**「DATA 无新鲜值 CONTEXT 诚实降级覆盖 + 后续 REPLAY 重播降级文本」用例，禁止「或等价」、禁止扩大代码范围）
5. 报告（冻结绝对路径）：
   - `D:\workbuddy_project\项目\可穿戴健康辅助系统\Code文档\自测报告\2026-09-01-跨路径上下文联动修复自测报告_cdx.md`
   - `D:\workbuddy_project\项目\可穿戴健康辅助系统\协同工作文档\完成报告\2026-09-01-跨路径上下文联动修复-完成报告_cdx.md`

### 明确禁止写入

- `voice-bridge/app/llm.py`（HermesLLM/LightweightLLM 不改，历史仍各自管，快照在 pipeline 层）
- `voice-bridge/app/tts.py`、`asr.py`、`vad.py`、`splitter.py`、`health.py`、`config.yaml`、`run.py`
- 固件、MAX30102、BLE 帧、告警门、TTS/ASR、直连策略（trust_env）
- 不新增依赖/API/配置/持久化；不落盘快照；不写敏感全文日志
- `ISSUE-0013` 文件本身（本轮不关闭，关闭在 Hermes 审查 + 真机验收后）
- Git commit/push/reset/clean；真机烧录/重启/采样

## 3. 精确操作（逐文件）

### 3.1 `router.py` — phrase-level 分类（S4）

- 新增 `REPLAY_PHRASES` / `CONTEXT_PHRASES` 复合短语常量 + `classify_followup(text) -> "replay" | "context" | None`。
- 只用完整复合短语匹配；**禁止**把「一遍/一次/重新/再说/重复/刚才/那个」等单 token 作为 hint 宽泛命中。
- 负例必须判 None：`查询一次天气`、`说一遍注意事项`、`再查一次天气`、`说一下天气`、`讲个笑话`、`谢谢`。
- 既有 ISSUE-0010 `_is_followup` 逻辑**保持不变**（分层独立）。

### 3.2 `pipeline.py` — 快照 + REPLAY 执行模式 + CONTEXT（S1/S2/S3/S5/S7）

- `StreamingPipeline.__init__` 加：`self._last_round: dict | None`（user_text/reply_text/route/ts）、`self._last_round_lock = threading.Lock()`、`SNAPSHOT_TTL_SECONDS=600`、`CONTEXT_REPLY_MAX_CHARS=500`、两个诚实模板常量。
- **REPLAY 执行模式**（S1，独立分支，非只改 timing）：命中 replay + 快照有效 → `route="replay"`、`selected_llm=None`、`final_text=reply_text`、`llm_backend="replay"`、`delta_iter=iter([final_text])`（分句+TTS，零 LLM/网络）；因 `route != HERMES` 不排 HERMES comfort、触发 TTS 预连接；绝不调 `stream_chat`。
- **CONTEXT 延续**（S7 逐路由闭合）：命中 context + 快照有效 → 构造固定结构前缀，延续 last_route 单次调用（HERMES→self.llm；LIGHTWEIGHT→lightweight_llm；RAG→不重检不包装、单次 lightweight；DATA→不送 LLM，复用 `_build_health_reply` 或诚实降级）。
- 上下文前缀固定结构、仅一轮、长度上限 500、不写日志。
- **降级**（S3）：replay/context 无快照 → 固定本地诚实模板，不调 LLM/网络。
- **提交时点**：完整回复后（yield 完所有帧 + assistant_parts 拼接后）写快照；首帧前不触碰。
- **写入规则**（S2）：普通成功轮覆盖；成功 CONTEXT 覆盖；REPLAY 不覆盖不刷新 ts；失败/取消保留旧快照；`last_route` 仅允许 LIGHTWEIGHT/RAG/HERMES/DATA。
- **锁**（S5）：只包 O(1) 内存复制，不持锁做 I/O/LLM/TTS。

### 3.3 测试（`test_router.py` + `test_pipeline.py`）

- `test_router.py`：phrase 分类用例（replay/context 命中 + S4 负例判 None + 既有 ISSUE-0010 用例不回归）。
- `test_pipeline.py`（R3 仅追加用例，业务代码不改）：在既有测试上**新增一条**「DATA 成功快照 → 数据变旧 → CONTEXT 回 `CONTEXT_NO_SNAPSHOT_TEXT` 并覆盖快照 → 后续 REPLAY 重播降级文本而非旧数值」用例，其余用例保持。

## 4. 基线冻结（S5/S6，实施前实跑，不硬编码）

- **正确性基线**：实施前实跑 `pytest tests -q`，记录真实 (passed/failed/skipped/warning) 计数冻结为基线；实施后退出码 0 且**不新增 fail/skip/warning**（**禁止硬编码 172**）。
- **延迟基线**：实施前冻结普通 LIGHTWEIGHT / HERMES comfort / DATA 三路径 `first_frame_ready_ms` 基线 + 真机全链路首字红线 **1484.589ms**。
- **脏树基线**：实施前记录 `git rev-parse HEAD` + `git status --porcelain` 全文 + whitelist 外既有脏项逐文件 SHA256；实施后证明 whitelist 外脏项未变；**不要求全局 diff 纯净**。
- **task-owned whitelist**：router.py / pipeline.py / test_router.py / test_pipeline.py / 2 份 _cdx 报告。

## 5. 首字证据方法（S5）

- **本地门**：REPLAY `first_frame_ready_ms` ≤ 同文本普通 lightweight + **50ms**；另证无 LLM 调用（llm_backend=replay + 无 stream_chat）+ 首帧门。
- **真机门**：全链路首字 ≤ **1484.589ms**，普通路径同条件不得回退。
- 命令 `pytest tests -q`；测量 N=10 轮；统计 median + p95；容差 +50ms；本地门/真机门边界分离记录。

## 6. 回滚

- 仅回滚 `router.py` + `pipeline.py` + 新增测试文件，保留修改前哈希与补丁；禁止 reset --hard / 整树覆盖。

## 7. 验收 DoD（对齐 Spec §7）

A) Spec §6 全部用例通过；全量 `pytest tests -q` 退出码 0（相对基线不新增 fail/skip/warning）；
B) 首字证据：本地门（REPLAY ≤ +50ms、无 LLM 调用）+ 真机门（全链路 ≤ 1484.589ms）；
C) 脏树证明：whitelist 外既有脏项 SHA256 未变（不要求全局 diff 纯净）；
D) 无落盘快照、无敏感全文日志、无依赖/API/配置新增。

## 8. 下一门

codex 读本任务单 + Spec 后**直接进入 IMPLEMENTATION_R3_REVIEW_PENDING**：业务代码（router.py/pipeline.py）无需修改（现状已符合 Spec）；仅在 File Map 内 `voice-bridge/tests/test_pipeline.py` 追加「DATA 无新鲜值 CONTEXT 诚实降级覆盖 + 后续 REPLAY 重播降级文本」用例，更新 _cdx 自测报告/完成报告后交 Hermes R3 独立审查。不得改源码/测试以外的文件、Issue、Git 或真机状态。

---

*Hermes（deepseek-v4-pro）· 2026-09-01 · 后缀 _hm 遵循 agent.md §5.1*
