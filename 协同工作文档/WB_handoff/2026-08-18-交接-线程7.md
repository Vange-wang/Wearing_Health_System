# 会话交接：2026-08-18 · 线程 7

## 项目
可穿戴健康辅助系统

## 交接原因
当前线程上下文过长（8-16 晚接手至今，跨 3 天，涵盖延迟优化、实时查询、句间连读、mDNS、看门狗、低功耗休眠、摔后排查等十几个大项，数十轮排障），token 接近上限，需开新线程继续。

## 一句话现状
**语音交互主链路全部落地并通过 Hermes 审查**（首字延迟 ≤2s 达标、路由智能、人设小V、句间连读修复、mDNS、看门狗、开机自启）；**低功耗休眠已实现验证走通但因 Vange 摔设备后异常、按 Vange 要求回退**（Spec 已就绪，待重新实现）；设备摔后确认功能正常（WiFi 天线接触不良丢包 37%，不影响基本对话）。

## 已完成（本线程内）

- [x] **首字延迟优化全闭环（硬需求 ≤2s 达成）**：方案1 DeepSeek 心跳保温 + 方案2 edge 流式首帧 + 方向1 TTS 预连接（vendored edge WSS 协议）+ 方向3 首段阈值 800 samples。实测 open_ms 均值 **1512ms（10 轮复测 1561ms）**，tts_first 930→383ms。Hermes 第 1 轮审查 **REVIEW_PASS** + 落地验收确认（`协同工作文档/审查报告/2026-08-17-首字延迟优化-落地验收确认_hm.md`）
- [x] **提示音（ack）演进全记录**：分类匹配（health/query/casual）→ 删闲聊 ack → 问候语不应声 → 快路径 ack 全删（首字达标后）→ 慢路径保留「稍等我帮你查一下，请稍等」安抚语；能力询问（能不能查天气）识别走快路径
- [x] **实时查询能力缺口闭环**：C 诚实兜底（系统提示词第 6 条防编造）+ A tool_seen 检测修复（改 httpx 直读 SSE 监听 `hermes.tool.progress` 事件，原 OpenAI SDK 抓不到非标准事件）+ ISSUE-0010 会话级路由记忆（「帮我查东莞天气→深圳的呢」追问延续慢路径防跌回编造）
- [x] **人设小V + 情感化**：助手名 小衡→小V；SYSTEM_PROMPT 情感化（活泼亲切带语气词）；edge 语速 -5%。SSML express-as 风格**不可行**（edge-tts 7.2.8 移除自定义 SSML，实测 39s 故障），已回滚，build_ssml 死代码已删
- [x] **慢路径诊断**：Hermes `streaming.enabled=false` 是 18s 首 token 根因（非 pro/flash 模型问题）→ Hermes 侧已开 streaming → 复测「你好」首 token 2.2~2.5s 达标；「查天气」10s 是联网搜索固有成本
- [x] **句间连读修复**（Spec `规划文档/Spec文档/2026-08-17-句间连读修复-spec_hm.md`）：分句集补 `～`(U+FF5E)+`…`(U+2026)（原只有 `〜` U+301C 码位不匹配）、兜底对齐软边界防词中腰斩、提示词加句号约束。真机听感 Vange 确认「足够了」，#4 语义分级停顿不做（登记 ISSUE-0012）
- [x] **mDNS 方案（治本 IP 漂移）**：固件 SERVER_URL 改 `http://voicebridge.local:8710`（不再写死 IP，换 WiFi 不用重烧）；电脑侧 `voice-bridge/mdns_advertise.py` 常驻广播 + 自启集成；固件侧 ESP-IDF v5.2.7 的 mdns 组件已移到组件管理器（`main/idf_component.yml` 声明 `espressif/mdns`，CMakeLists 恢复空 REQUIRES 保留隐式依赖链）。端到端验证通过
- [x] **看门狗自动复位**：`voice_agent.c` WiFi 连续断开 10 次（s_wifi_reconnect_fail ≥ 10）→ `esp_restart()`，GOT_IP 清零。假死自动恢复不用手动按 reset
- [x] **开机自启**：启动文件夹 `voice-bridge-autostart.bat`（GBK 编码防中文乱码；坑：`start ... >> log` 重定向不传递，需 `cmd /c` 内嵌）；服务日志改落 `voice-bridge/logs/voice-bridge.log`
- [x] **低功耗休眠实现+验证后回退**：按 Spec `2026-08-17-低功耗休眠-spec_hm.md` 实现浅睡（enter_light_sleep：关 codec→停 WiFi→ext1 唤醒→esp_light_sleep_start→唤醒恢复 WiFi/mDNS/codec），关键坑：**ext1 必须 `rtc_gpio_init` + `rtc_gpio_set_direction(INPUT_ONLY)` + `rtc_gpio_pullup_en` 切 RTC 域配好上拉**（官方 ext_wakeup.c 佐证），按键唤醒第 6 秒 ping 通验证成功。**后因 Vange 摔设备出现异常，按 Vange 要求全部回退**（voice_agent.c 撤销 enter_light_sleep/IDLE_SLEEP_SECONDS/s_last_active/s_fs/rtc_gpio/esp_sleep/esp_timer 相关，固件 git diff 只剩 21 行 mDNS+看门狗改动，编译 exit 0 已烧录）
- [x] **摔后排查定案**：设备一切正常（能 boot、连 WiFi、对话，服务端日志实锤「你好」open_ms 1581ms 达标）。「没反应」真相 = 摔后 WiFi 天线接触不良（ping 丢包 37% 时好时坏）+ 我抓串口时机错误（esptool 复位后 USB 重枚举需 ≥5 秒，等 1~3 秒抓串口误判「下载模式/CDC 故障」——**教训：复位后抓串口必须等 ≥5 秒**）
- [x] **复盘文档交付**：583 行《语音输出调试全过程复盘》已出 md + docx 版放 Vange 桌面（`D:\UserData\86166\KnownFolders\Desktop\2026-08-17-语音输出调试全过程复盘_wb.docx`）
- [x] **代码提交**：4 个主题 commit（仓库卫生/首字延迟/路由智能/人设LLM）+ test_router.py(7用例) + 10轮复测脚本报告 + README 部署检查 + 句间连读修复 2 commit。测试全量 45 passed + 7 skipped

## 未完成（新线程继续，按优先级排序）

- [ ] **P1 git 收尾**：工作区有未提交改动（见下「关键文件」标 ?? 的 + ISSUE 总表 M），按主题补 commit 提交（低功耗休眠方案对齐_wb / spec_hm 属历史文档，与 mdns_advertise.py/start-voice-bridge.bat 一起按「mDNS+自启+低功耗休眠文档」主题收）
- [ ] **P1 服务端/设备状态快检**：确认 8710 服务端、8780 Hermes、mDNS 5353、设备 WiFi（ping -n 5 看丢包率）四者在跑，自启 bat 生效
- [ ] **P2 低功耗休眠重新实现**（Vange 拍板过：浅睡 + 3 分钟 + 按键唤醒，为电池供电准备）：Spec 已就绪（`规划文档/Spec文档/2026-08-17-低功耗休眠-spec_hm.md`），实现路径已摸通（见已完成低功耗休眠条目 + 关键坑 rtc_gpio_init）。**先与 Vange 确认是否重做**（因摔后回退，硬件稳定性待观察）；重做时 IDLE_SLEEP_SECONDS 直接 180，不走 30 临时值
- [ ] **P2 ISSUE-0001 留证复核**：慢路径安抚语第一帧已留证（comfort_sent=true，route=hermes 触发），待 Hermes 复核转 closed
- [ ] **P2 ISSUE-0010 复核**：会话级路由记忆已实现+端到端验证（东莞天气→深圳的呢均 route=hermes），待 Hermes 复核转 closed
- [ ] **P2 慢路径提速**（Hermes 侧，voice-bridge 无解）：查天气等联网搜索 10~18s 固有成本，可优化 reasoning_effort / 搜索后端；需与 Hermes 对齐
- [ ] **P3 ISSUE-0009**：ASR 真人发音基线已记录（11/15 完全一致、2 近义、2 硬错误：体温→气温/烧到→收到），需决定是否做热词表/后处理纠错（Hermes 选型）
- [ ] **P3 ISSUE-0011**：TTS 情感表达换引擎立项（CosyVoice/火山/GPT-SoVITS），SSML 不可行后情感化治本需换引擎，等 Hermes+zcode 出选型对比
- [ ] **P3 ISSUE-0012**：语义分级停顿（话题切换 ≥0.5s 长停顿），句间连读 Spec §4 承诺的后续项，待立项
- [ ] **P3 技术债**：main.py `@app.on_event` 是 FastAPI deprecated 用法（Hermes 审查 NON_SERIOUS #2），lifespan 迁移可顺手做
- [ ] **P3 设备天线**：摔后 WiFi 丢包 37%，建议 Vange 物理检查天线/靠近路由器；若持续影响语音需考虑硬件处理

## 阻塞点 / 待决策

- **低功耗休眠是否重做**：等 Vange 拍板（Spec 已就绪，实现路径已通，但摔后回退了；设备硬件稳定性待观察）
- **设备 WiFi 天线**：摔后接触不良（丢包 37%），硬件问题 WorkBuddy 无法修，需 Vange 物理检查；若影响语音，建议靠近路由器
- **ISSUE-0001/0010 转 closed**：等 Hermes 复核（证据已留证）
- **TTS 换引擎 / 慢路径提速**：等 Hermes + zcode 选型/对齐

## 关键文件 / 路径

- 协作规范：`D:\workbuddy_project\项目\可穿戴健康辅助系统\agent.md`（v1.5：zcode 角色 + 文件命名 `_wb/_hm/_zc` 后缀 + 3 轮审查上限）
- Spec 低功耗休眠：`D:\workbuddy_project\项目\可穿戴健康辅助系统\规划文档\Spec文档\2026-08-17-低功耗休眠-spec_hm.md`
- Spec 句间连读：`D:\workbuddy_project\项目\可穿戴健康辅助系统\规划文档\Spec文档\2026-08-17-句间连读修复-spec_hm.md`
- 延迟优化查证/复测：`D:\workbuddy_project\项目\可穿戴健康辅助系统\规划文档\技术验证\2026-08-17-首字延迟优化查证_wb.md`、`2026-08-17-首字延迟优化落地复测_wb.md`、`2026-08-17-首字延迟落地复核与TTS关键缺陷_zc.md`
- 服务端代码：`D:\workbuddy_project\项目\可穿戴健康辅助系统\voice-bridge\app\`（llm.py 心跳保温/tts.py 预连接/pipeline.py 流式产帧/router.py 路由智能/splitter.py 分句器）
- 服务端配置：`D:\workbuddy_project\项目\可穿戴健康辅助系统\voice-bridge\config.yaml`
- mDNS 广播：`D:\workbuddy_project\项目\可穿戴健康辅助系统\voice-bridge\mdns_advertise.py`（未提交，待入库）
- 自启脚本：`D:\workbuddy_project\项目\可穿戴健康辅助系统\voice-bridge\start-voice-bridge.bat`（未提交，待入库）；启动文件夹 `voice-bridge-autostart.bat`
- 固件代码：`D:\esp-box\examples\voice_agent\main\voice_agent.c`（git diff 只剩 21 行 mDNS+看门狗，低功耗休眠已回退）；组件依赖 `main/CMakeLists.txt` + `main/idf_component.yml`（espressif/mdns）
- 固件工具脚本：`D:\esp-box\build_check.py`（编译）、`D:\esp-box\flash_only.py`（烧录）、`D:\esp-box\wait_flash2.py`（等下载模式烧录）、`D:\esp-box\reset_capture.py`（复位抓日志，**注意：复位后必须等 ≥5 秒再抓串口**）
- issue 库：`D:\workbuddy_project\项目\可穿戴健康辅助系统\协同工作文档\ISSUE\`（总表 + Open_Issue/ISSUE-0001/0009/0010/0011/0012）
- 低功耗休眠方案对齐（未提交）：`D:\workbuddy_project\项目\可穿戴健康辅助系统\协同工作文档/通知与对齐\2026-08-17-低功耗休眠-方案对齐_wb.md`
- ISSUE-0012（未提交）：`D:\workbuddy_project\项目\可穿戴健康辅助系统\协同工作文档\ISSUE\Open_Issue\ISSUE-0012-语义分级停顿.md`
- 复盘文档：`D:\UserData\86166\KnownFolders\Desktop\2026-08-17-语音输出调试全过程复盘_wb.docx`
- 项目记忆：`D:\workbuddy_project\项目\可穿戴健康辅助系统\.workbuddy\memory\2026-08-16.md`、`2026-08-17.md`（最全，含低功耗休眠实现细节与摔后排查全过程）

## 下一步动作（新线程开工第一步）

1. **读本交接文档 + 最近 memory**（`D:\workbuddy_project\项目\可穿戴健康辅助系统\.workbuddy\memory\2026-08-17.md` 尾部，含低功耗休眠实现细节/摔后排查/教训）
2. **快检全链路**：8710 服务端 health、8780 Hermes、5353 mDNS、设备 ping（-n 5 看丢包）、自启 bat 存在；服务端挂了用 `cd voice-bridge && venv/Scripts/python.exe run.py`（日志落 `logs/voice-bridge.log`）拉起
3. **git 收尾**：把未提交的 mdns_advertise.py、start-voice-bridge.bat、低功耗休眠方案对齐_wb、低功耗休眠 spec_hm、ISSUE-0012、ISSUE 总表 M 按主题补 commit
4. **问 Vange**：低功耗休眠是否重新实现（Spec 已就绪，实现路径已通）；确认后按 Spec 重做（IDLE_SLEEP_SECONDS=180，含 rtc_gpio_init 关键配置）
5. 其余 P2/P3 issue 按上面优先级推进，涉及 Hermes 的（复核/选型/慢路径）用 hermes CLI 或等 Vange 转

## 关键规范提醒

- **模型守卫**：代码开发动工前向 Vange 核实模型（本线程已核实：Kimi K3 无额度，WorkBuddy 开发暂用 **deepseek-v4-pro**，已记录 agent.md；用户换 WorkBuddy 平台模型≠改项目配置，别动 config.yaml/hermes/box3）
- **文件命名**：WorkBuddy 产出文档一律带 `_wb` 后缀（Hermes `_hm`、zcode `_zc`）
- **审查报告落盘**：`协同工作文档/审查报告/`
- **issue 库**：`协同工作文档/ISSUE/`（ISSUE-XXXX 编号，Hermes 登记、WorkBuddy 执行、Vange 拍板）
- **协作规范**：agent.md v1.5（3 轮审查上限、任务单、DoD、OI 关闭自动化、Session 铁律）
- **用户全局偏好**：文件输出 D 盘、交付物回复给完整路径（纯文本，不用 markdown 链接）、路径不得省略
- **环境坑**：PowerShell 直接输出常被吞（写文件再读）；safe-delete 钩子会拦 rm（中文路径用 Python os.remove）；esptool 复位后抓串口等 ≥5 秒；bat 中文路径 GBK 编码
