# 项目迁移执行任务单（zcode）

> **⚠️ 状态：已暂缓（2026-08-19 Vange 拍板）**
> 依据 `2026-08-19-项目迁移可行性分析_zc.md`：风险中等可控，但迁移收益仅为目录名归属象征（项目归属由 git 远程 `Vange-wang/Wearing_Health_System` 决定，与本地目录名无关），成本是服务中断 + venv 版本漂移风险 + 会话 cwd 中断。**Vange 决定暂缓，项目保持现路径 `D:\workbuddy_project\项目\可穿戴健康辅助系统`**。如未来有实质迁移必要（目录规范/多项目隔离），按本任务单执行，且必须遵守可行性分析 §二 规避方案（先测 venv 不重建、重建则锁版本）。

- **决策方**：Vange（用户）· 2026-08-19
- **任务**：将项目文件夹 `D:\workbuddy_project\项目\可穿戴健康辅助系统` 整体迁移到 `D:\zcode_project\可穿戴健康辅助系统`，并修复所有路径引用
- **执行方**：zcode（开发员）
- **完成标志**：项目在新路径正常运行（git 完好 + voice-bridge 可启动 + 自启指向新路径 + 文档引用无残留）

---

## 一、前置检查（迁移前）

1. **停 voice-bridge 服务**：若 8710 端口在监听（`netstat -ano | findstr 8710`），先停掉——文件被占用会导致移动失败。
2. **git 工作区干净**：`git status` 无未提交变更（有则先提交或 stash，防止迁移中丢失）。
3. **目标目录确认**：`D:\zcode_project` 存在（不存在先创建）；确认源/目标同盘（都是 D:，`mv` 瞬时完成，不走复制）。

## 二、迁移步骤

### A. 移动文件夹

```bash
mv "D:\workbuddy_project\项目\可穿戴健康辅助系统" "D:\zcode_project\可穿戴健康辅助系统"
```

- 同盘 D:，瞬时完成；Git 历史、remote、venv 目录整体搬走。
- 移动后确认：`D:\zcode_project\可穿戴健康辅助系统\.git` 存在、`git log` 正常。
- `D:\workbuddy_project\项目\` 下其他内容不受影响（只移了这一个子文件夹）。

### B. 路径引用修正（关键，漏一处就断一处）

1. **开机自启 bat**（项目外，必须改）：
   - 路径：`C:\Users\86166\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\voice-bridge-autostart.bat`
   - 内容：`cd /d "D:\workbuddy_project\项目\可穿戴健康辅助系统\voice-bridge"` → `cd /d "D:\zcode_project\可穿戴健康辅助系统\voice-bridge"`
   - ⚠️ **GBK 编码重写**（cmd 兼容中文路径，用 Python `open(..., encoding='gbk')` 写，别用 UTF-8）
   - 读回验证：中文路径无乱码。

2. **voice-bridge venv**（移动后 venv 的 exe shebang 断裂）：
   - 现象：`venv\Scripts\pip.exe` 等内部 shebang 指向旧路径，直接跑报错。
   - 处理：**删除重建** venv（最干净）：
     ```bash
     cd "D:\zcode_project\可穿戴健康辅助系统\voice-bridge"
     rm -rf venv
     python -m venv venv
     venv\Scripts\pip install -r requirements.txt
     ```
   - 验证：`venv\Scripts\python.exe -c "import app"` 或启动服务 health ok。

3. **项目内文档路径引用**（Spec/审查报告/ISSUE/交接文档等）：
   - 全量搜索：`grep -r "D:\\workbuddy_project\\项目\\可穿戴健康辅助系统" .`（含 .md/.yaml/.py）
   - 批量替换为 `D:\zcode_project\可穿戴健康辅助系统`（Python 脚本处理，UTF-8 读写的文件用 UTF-8，GBK 的另处理）。
   - 替换后复查：`grep -r "workbuddy_project" .` 应只剩历史记录类（如 WB_handoff 交接文档里"原路径"的叙述性引用可保留，标注旧路径即可——以 zcode 判断，功能性引用必改）。

4. **voice-bridge 配置内部**：检查 `voice-bridge\config.yaml`、启动脚本有无绝对路径（`D:\workbuddy_project\...`），有则改。

5. **Hermes 侧检查**：`~/.hermes` 下（cron/脚本/gateway 配置）有无指向旧项目路径的引用（grep `workbuddy_project`），有则改；没有则跳过。

### C. 验证（完成标志）

1. `git status`/`git log`：仓库完好，历史保留。
2. voice-bridge 启动：`venv\Scripts\python.exe run.py` → health ok（8710 监听）。
3. 自启 bat：读回内容指向 `D:\zcode_project\可穿戴健康辅助系统\voice-bridge`（GBK 无乱码）。
4. 文档引用：`grep -r "workbuddy_project"` 项目内无功能性残留。
5. 端到端（可选）：真机对话一次确认语音链路正常。

## 三、风险与注意

| 风险 | 缓解 |
|---|---|
| 移动时服务占用 → 失败 | 前置先停服务（步骤一） |
| venv 断裂（pip/exe shebang） | 删除重建 + 重装依赖（步骤 B2） |
| 文档路径引用遗漏 | grep 全量 + 替换后复查（步骤 B3） |
| 自启 bat 中文乱码 | GBK 编码写 + 读回验证（步骤 B1） |
| 迁移后旧路径残留进程 | 迁移前停净；迁移后 `netstat 8710` 确认无旧实例 |

## 四、完成后回报

一句确认：迁移完成 + 验证项 1-4 通过（或哪项失败 + 原因）。

---

*Hermes（deepseek-v4-pro）· 2026-08-19 · 后缀 _hm 遵循 agent.md v1.6 §5.1*
