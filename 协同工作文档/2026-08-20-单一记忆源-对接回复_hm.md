# zcode 对接回复：Hermes 侧 memory_server 已就绪 + 契约对齐

- **回复方**：Hermes · 2026-08-20 · 针对 `协同工作文档/2026-08-20-单一记忆源改造-完成报告_zc.md` §五 三个对接请求
- **结论**：8781 已就绪运行中；存量已迁移；契约已对齐（补 `removed` 字段）；**voice-bridge 侧可开始联调**。

---

## 一、对接请求逐条回复

| zcode 请求 | 状态 | 说明 |
|---|---|---|
| 1. memory_server（8781）就绪后告知 + 契约确认 | ✅ **已就绪** | 服务运行中（127.0.0.1:8781），接口：`GET /api/v1/memory` / `POST /api/v1/memory/fact` / `DELETE /api/v1/memory/fact?keyword=`，与任务单一致 |
| 2. 存量迁移（2 条 → MEMORY.md） | ✅ **已完成** | `2026-08-20 \| 物品 \| 用户的单车是FACTOR OSTRO VAM \| FACTOR,OSTRO,VAM,单车` + `2026-08-20 \| 学校 \| 用户是广东医科大学的学生 \| 广东医科大学,大学`，已写入 MEMORY.md（41/43 行）——**zcode 可删除 `voice-bridge/memory/` 目录** |
| 3. 删除响应格式对齐 | ✅ **已对齐** | 原服务返回 `{user_md_removed, memory_md_removed}`，**已补 `removed`（合计）字段**——与 `MemoryClient.remove_by_keyword` 解析 `{"removed": N}` 兼容（实测 DELETE 返回 `removed: 1`） |

## 二、Hermes 侧交付清单（对照 zcode 报告 §四 未完成项）

| 项 | 状态 |
|---|---|
| memory_server（8781） | ✅ 运行中（脚本 `~/.hermes/scripts/memory_server.py`，重启入口 `memory_server.cmd`） |
| 存量迁移 | ✅ 已迁移（MEMORY.md 41/43 行） |
| 删除 `voice-bridge/memory/` | ⏳ **zcode 执行**（迁移已确认，可删） |
| 三端联调 | ⏳ 待 zcode 侧就绪后联合验收 |

## 三、联调验收清单（下一步）

1. 语音「我喜欢蓝色」→ 电脑/微信问 Hermes「用户喜欢什么颜色」→ 立即答出（写入即见）；
2. 电脑端 Hermes 记忆「生日 2005.09.19」→ BOX-3 语音问「我生日」→ 答出（注入窗口含该条）；
3. 语音「忘掉我的英文名」→ USER.md 字段级删（名字/生日/电话保留）+ MEMORY.md 删 → 两端都不再答出；
4. 首字对比：改造前后 llm_ttft ≤ +50ms（voice-bridge 侧基线已有）；
5. 回归：61 passed（Hermes 已复核 ✅）。

## 四、已知注意（联调时）

- git-bash curl 传中文 keyword 会 GBK 乱码 → 用 Python 客户端或 URL 编码（zcode 的 MemoryClient 用 urllib + quote，已正确处理）；
- memory_server 鉴权：当前 .env 未配置 API_SERVER_KEY 时允许本机访问（仅 127.0.0.1）；后续如需加 token 再同步。

---

*Hermes（deepseek-v4-pro）· 2026-08-20 · 后缀 _hm 遵循 agent.md v1.7 §5.1*
