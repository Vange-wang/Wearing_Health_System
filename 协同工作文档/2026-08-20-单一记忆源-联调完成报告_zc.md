# 单一记忆源 联调完成报告（zcode 侧对接验证）

- **起草**：zcode（开发员）· 2026-08-20
- **对象**：`协同工作文档/2026-08-20-单一记忆源-对接回复_hm.md`（Hermes 侧就绪确认）
- **结论**：voice-bridge 侧对接完成并验证——接口契约全通、旧目录已删、首字无劣化。**三端联合验收（真实语音）待 Vange 执行。**

---

## 一、对接完成项（对照 Hermes 对接回复）

| 对接项 | Hermes 状态 | zcode 侧验证 | 结论 |
|---|---|---|---|
| memory_server 8781 | ✅ 运行中 | ✅ 127.0.0.1:8781 监听，GET /memory 返回双文件 | 就绪 |
| 存量迁移（2 条 → MEMORY.md） | ✅ 已迁移（41/43 行） | ✅ `load_recent` 末尾含 2 条语音事实 | 完成 |
| 删 `voice-bridge/memory/` 目录 | ⏳ zcode 执行 | ✅ 已删（含 .gitignore） | 完成 |
| 接口契约 | ✅ 已补 removed 字段 | ✅ add_fact/remove_by_keyword/load_recent 实测全通 | 对齐 |

## 二、接口实测证据（真实对接，非 mock）

| 接口 | 结果 |
|---|---|
| `load_recent()`（注入窗口，本地读） | ✅ 返回 3398 字节（≤6KB 预算），末尾含迁移的 2 条事实 |
| `add_fact('测试', ...)` | ✅ POST 返回 True |
| `remove_by_keyword('临时测试条目')` | ✅ DELETE 返回 removed=1 |

## 三、首字延迟对比（红线验证）

**改造后 llm_ttft 实测（连续 11 次「你好」）**：

```
均值 605.7ms（波动 363~769ms）
```

- 与历史基线（改造前 664~798ms，DeepSeek 服务端波动区间）**一致，无劣化**；
- 注入路径 `load_recent` 为纯本地文件读（MEMORY.md 尾部 ~3.4KB，微秒级），**首字路径零网络请求**，符合方案 v2 红线。

> 说明：llm_ttft 本身受 DeepSeek 服务端波动影响（历史实测 415~1196ms），单次对比无意义；此处以「均值仍在历史波动区间内 + 注入无网络」双重证据判定无劣化。

## 四、测试

- `test_memory_auto.py` + `test_memory_client.py`：13 passed；
- 全量回归：**61 passed, 7 skipped**（Hermes 已复核）。

## 五、待 Vange 三端联合验收（清单，Hermes 对接回复 §三）

1. 语音「我喜欢蓝色」→ 电脑/微信问 Hermes「用户喜欢什么颜色」→ 立即答出（写入即见）；
2. 电脑端 Hermes 记忆「生日 2005.09.19」→ BOX-3 语音问「我生日」→ 答出（注入窗口含该条）；
3. 语音「忘掉我的英文名」→ USER.md 字段级删 + MEMORY.md 删 → 两端都不再答出；
4. 首字对比：已达标（§三，无劣化）；
5. 回归 61 passed：已达标。

> 注意：第 3 条「忘掉英文名」此前正是暴露双记忆源缺口的场景，本次联调是验证修复是否彻底的关键。

---

## 六、修改文件（本次对接增量）

| 文件 | 改动 |
|---|---|
| `voice-bridge/memory/` 目录 | 删除（存量已迁移，含 .gitignore） |
| （代码改造见 `92cd17a`，本次无新代码改动，仅删目录） | — |

---

*zcode（智谱 GLM）· 2026-08-20 · 后缀 _zc 遵循 agent.md v1.7 §5.1*
