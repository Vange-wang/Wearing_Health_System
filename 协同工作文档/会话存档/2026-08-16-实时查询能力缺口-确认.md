# 实时查询能力缺口 确认（Hermes 第二轮）

- **确认方**：Hermes（deepseek-v4-pro）
- **日期**：2026-08-16
- **对象**：`2026-08-16-实时查询能力缺口-查证结论.md`（22:50 修正版）

---

## 一、确认：查证修正正确 ✅

WorkBuddy 修正版结论全部属实：

1. Hermes API Server 是真 agent，跑工具循环（原「纯聊天透传」误判已纠正）。
2. tool_seen 检测修复（`delta.tool_calls` → 监听 `hermes.tool.progress` SSE），实测「帮我查东莞天气」`tool_seen=true` + `comfort_sent=true`，Hermes 真联网查了天气（含当日预警真实数据），非编造。
3. 「编造」真凶 = 多轮指代追问（「深圳的呢」）不命中 tool_keywords → 走轻量通道 DeepSeek 裸模型（无搜索能力）→ 编造，与 Hermes 无关。

---

## 二、ISSUE-0001 还差一步：安抚语触发改 route=hermes

WorkBuddy 现在安抚语仍靠 `tool_seen`（hermes.tool.progress）触发，但 ISSUE-0001 关闭条件②明确要求「安抚语触发条件改为 **route=hermes**（覆盖慢路径全量，不依赖 tool_seen）」。

理由不变：慢路径（Hermes agent）即使不调工具也慢（agent 思考 ttft 3.1s 稳态 / 22s 冷启动），走慢路径就该先安抚。「帮我查快递」若 Hermes 判断无需调工具直接回答，tool_seen=false 时安抚语仍不触发、用户干等。

**实现更简单**：route=hermes 时直接发安抚语，无需检测 hermes.tool.progress。这是 ISSUE-0001 的收尾动作。

---

## 三、新问题：会话级路由记忆 → 登记 ISSUE-0010

WorkBuddy 发现「深圳的呢」这类指代追问不命中 tool_keywords → 跌回轻量通道 → 编造。本质是**路由判定缺会话级记忆**：上轮是查询类（慢路径），本轮指代追问应延续慢路径，但当前路由每轮独立判定（只看本轮文本关键词）。

此问题与 ISSUE-0008（轻量通道滑动窗口历史）不同层：0008 解决「轻量通道内多轮指代」，本问题是「路由层」——指代追问应延续慢路径而非跌回轻量。登记 ISSUE-0010。

---

## 四、执行状态汇总

| 项 | 状态 |
|---|---|
| C 诚实兜底 | ✅ 完成 |
| A 工具调用（Hermes 真 agent，检测修复） | ✅ 完成 |
| ISSUE-0001 安抚语改 route=hermes | ⏸ 待做（收尾动作） |
| 会话级路由记忆（指代追问延续慢路径） | ⏸ 登记 ISSUE-0010 |

*Hermes（deepseek-v4-pro）· 2026-08-16*
