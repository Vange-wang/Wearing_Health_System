# 单一记忆源改造（voice-bridge 侧）完成报告

- **起草**：zcode（开发员）· 2026-08-20
- **对象**：`zcode_handoff/2026-08-20-单一记忆源改造-任务单_hm.md`（v2 方案）
- **结论**：voice-bridge 侧改造完成（MemoryClient + 注入/提取/遗忘改造 + 单测 61 passed）。**Hermes 侧 memory_server（8781）未就绪，存量迁移与三端联调待 Hermes 侧完成后进行。**

---

## 一、改动清单（voice-bridge 侧）

| 文件 | 改动 |
|---|---|
| `app/memory.py` | `MemoryStore`（本地文件读写）→ `MemoryClient`：`load_recent()`（本地读 MEMORY.md 最近 ≤6KB，零网络）+ `add_fact()`（POST /fact）+ `remove_by_keyword()`（DELETE /fact?keyword=），失败静默重试 1 次 |
| `app/llm.py` | ① `_system_prompt` 注入改：USER.md 全文（本地直读，红线保留）+ `memory.load_recent()`（MEMORY.md 最近窗口）；② 提取/遗忘走 `MemoryClient.add_fact/remove_by_keyword`（方法名不变，逻辑无缝） |
| `app/main.py` | `MemoryStore(cfg.memory_user_facts_path)` → `MemoryClient(api_url, user_profile_path, memory_file_path, inject_budget)` |
| `app/config.py` | 新增 `memory_api_url`（默认 8781）、`memory_inject_budget`（默认 6144）、`memory_file_path`（默认 ~/.hermes/memories/MEMORY.md）；删除 `memory_user_facts_path` |
| `config.yaml` | memory 段改为 `api_url` / `inject_budget` / `file_path` |
| `tests/test_memory_auto.py` | 重写为 mock MemoryClient（记录 add_fact/remove_by_keyword/load_recent 调用） |
| `tests/test_memory_client.py` | 新增（MemoryClient 本地读取 6KB 截取 / 换行对齐 / 缺失静默） |

## 二、首字红线遵守

- **注入只读本地文件**（USER.md + MEMORY.md 本地路径），`load_recent` 纯文件读、无任何 HTTP；
- 提取/遗忘在回复完成后后台线程调用 HTTP，不阻塞首字路径。

## 三、测试

- `test_memory_auto.py` + `test_memory_client.py`：13 passed；
- 全量回归：**61 passed, 7 skipped**（改造前 60 passed）。

## 四、未完成（依赖 Hermes 侧）

| 项 | 状态 | 阻塞 |
|---|---|---|
| Hermes memory_server（8781） | ❌ 未就绪（端口未监听、脚本不存在） | Hermes 并行开发中 |
| 存量迁移（user_facts.md 2 条 → MEMORY.md） | ⏳ 待 Hermes 侧 HTTP 就绪后执行 | 依赖 8781 |
| 删除 `voice-bridge/memory/` 目录 | ⏳ 迁移确认后执行 | 依赖迁移 |
| 三端联调（写入即见 / 反向共享 / 遗忘全端失效） | ⏳ 待双方就绪 | 依赖 8781 |

## 五、给 Hermes 的对接请求

1. memory_server（8781）就绪后告知，接口契约请确认与任务单一致（GET /memory、POST /fact、DELETE /fact?keyword=）；
2. 存量迁移：`user_facts.md` 现 2 条（单车 FACTOR OSTRO VAM / 学校广东医科大学），请 Hermes 侧写入 MEMORY.md 后告知，zcode 删 `voice-bridge/memory/` 目录；
3. 字段级删除的响应格式（`removed` 计数）请与 `MemoryClient.remove_by_keyword` 的解析对齐（当前解析 `{"removed": N}`）。

---

*zcode（智谱 GLM）· 2026-08-20 · 后缀 _zc 遵循 agent.md v1.7 §5.1*
