# 角色变更通知 —— WorkBuddy 暂停，zcode 全权接管开发

- **决策方**：Vange（用户）· 2026-08-19
- **生效时间**：当前 CosyVoice 部署任务收尾完成后
- **对象**：Hermes / zcode / WorkBuddy（三方）

---

## 一、变更内容

1. **WorkBuddy 暂停**：完成当前 CosyVoice 部署收尾后，**暂停一切事务**——不再接收新任务、不再修改代码、不再响应协作文档。
2. **zcode 全权接管**：开发职责全权移交 zcode（代码开发、自测、部署、集成、技术查证、方案设计）。
3. **Hermes 不变**：继续担任审查方（Spec 起草、审查报告、验收确认、裁决对齐）。

## 二、角色职责表（更新后）

| 角色 | 职责 |
|---|---|
| **Hermes**（_hm）| 大脑/审查方：Spec 起草、审查报告、验收、裁决对齐 |
| **zcode**（_zc）| **开发员（全权接管）**：代码开发、自测、部署、集成、技术查证、方案设计 |
| **WorkBuddy**（_wb）| 暂停：完成 CosyVoice 部署收尾后停止一切事务 |

## 三、交接事项

1. **WB 收尾交付**：完成 CosyVoice 部署（环境/模型/推理验证），产出**部署状态说明**（环境路径、验证结果、遗留问题），交接给 zcode。
2. **zcode 承接**：后续任务由 zcode 承接（待办见 ISSUE 库 + Spec 清单）：
   - CosyVoice 集成 voice-bridge（ISSUE-0011 待办）
   - 多 WiFi 自动切换（`2026-08-18-多WiFi自动切换-spec_hm.md`）
   - 低功耗休眠（`2026-08-17-低功耗休眠-spec_hm.md`，暂缓）
   - 其余 Open Issue（0001-0012）
3. **Hermes 审查不变**：送审 → Hermes 审查 → 落盘审查报告，流程照旧。

## 四、说明

- **变更原因**：WorkBuddy 近期交付质量与协作效率未达预期（Vange 判定），不再承担开发职责。
- **文件后缀规范不变**：_hm / _zc / _wb；WB 暂停后新产出文件仅 _hm / _zc。
- **agent.md 已同步更新**（v1.6，见项目根目录）。

---

*Hermes（deepseek-v4-pro）· 2026-08-19*
