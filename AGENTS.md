# AGENTS.md

> # 🚨🚨🚨 最高铁律（Vange 亲定，违反即返工）🚨🚨🚨
>
> **所有决定都不能以牺牲首字输出延迟为代价——除非后续可以百分百确定补回来或者优化更多。**
>
> 任何方案、设计、改动（记忆系统、搜索、路由、TTS、上下文等一切功能）必须先过这一关：
> 首字延迟（用户开口到听到第一个字的时长）是体验生命线，不可为任何功能收益而牺牲；
> 只有在能确定性补偿或净优化更多的前提下才允许偏离。此条优先级高于本文件一切其他条款。

本项目是"可穿戴健康辅助系统"（毕业设计）的研发协作仓库：可穿戴传感节点（ESP32）采集生理数据，经 BLE 汇聚到便携式 AI 终端（ESP32-S3 语音板），终端通过 WiFi 接入 Hermes 智能体（大脑），调用 DeepSeek 实现语音交互与健康分析。

## 协作分工（最高优先级）

- **Hermes（总指挥/大脑）**：负责方向、架构设计、方案与 Spec 定义、技术选型、代码审核、联调验收。不写业务代码。
- **WorkBuddy（开发员）**：负责按 Spec 实现代码（语音桥服务、开发板固件等）。产出代码必须经 Hermes 审核通过后合入。
- 未经 Spec 确认的功能不得擅自开发；未经 Hermes 审核的代码不得宣布完成。

## 开发纪律（强制执行）

1. **先 Spec 后代码**：动手前必须阅读 `规划文档/Spec文档/` 中的对应 Spec；Spec 未覆盖的需求，先写进 Spec 再开发。
2. **DeepSeek API 与微信 iLink 必须直连**，任何环境（含服务器）不得配置代理。
3. **软件一律安装到 D 盘**（C 盘紧张）。
4. **阶段边界不可逾越**：v0.1 只做独立语音服务，禁止提前接入 Hermes 集成（那是 v0.3 的内容）；v0.2 之前禁止引入流式复杂度。
5. **可插拔设计**：ASR / TTS / LLM 必须走抽象接口，更换引擎只改配置不改业务代码。
6. **实测数据说话**：任何延迟/性能结论必须有打点日志或实测报告，禁止"应该没问题"式结论。

## 文档优先级（冲突时按此顺序）

1. `规划文档/Spec文档`：要做什么、为什么、验收标准（本仓库最高裁决）
2. `规划文档/里程碑文档`：分阶段交付范围与验收
3. `规划文档/技术验证`：技术选型、可行性验证、风险结论
4. `Code文档`：代码结构、接口、部署、运行说明
5. `AGENTS.md`：协作规则

## 重要目录职责

- `规划文档/Spec文档`：功能 Spec，命名 `YYYY-MM-DD-功能名-spec.md`
- `规划文档/技术验证`：选型结论与风险记录
- `规划文档/里程碑文档`：阶段交付计划
- `Code文档`：代码结构、接口约定、开发员工作记录
- `协同工作文档`：跨角色沟通、验收记录
- `总负责人文档`：Hermes 的决策记录
- `voice-bridge/`：语音桥服务源码（当前阶段唯一代码目录）

## 项目当前状态

- 阶段：v0.1 语音桥服务开发前（Spec 已就绪）
- 技术栈已定：Python 3.11 / FastAPI / sherpa-onnx(SenseVoice) / edge-tts + piper / DeepSeek
- 项目根目录已 git init（本地仓库，尚未关联远程）
