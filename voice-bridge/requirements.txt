# voice-bridge 运行依赖（v0.2 继承 + v0.3/长期RAG：numpy + jieba）
fastapi>=0.110
uvicorn>=0.29
sherpa-onnx>=1.10.30
edge-tts>=6.1,<7.0  # 7.x 移除 output_format（固定24k mp3），6.1.x 支持直接输出 16kHz WAV
piper-tts>=1.2.0
openai>=1.30
python-multipart>=0.0.9
PyYAML>=6.0
numpy>=1.24  # VAD RMS 计算（v0.1 已通过 sherpa-onnx 间接依赖）
jieba>=0.42  # 长期 RAG：BM25 关键词检索分词（唯一新增依赖，RAG Spec §2 红线）
