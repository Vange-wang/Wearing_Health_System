# v0.4 真机固件联调自测报告

> 日期：2026-08-16 · 版本：voice-bridge v0.4 + voice_agent 固件 · 依据：`规划文档/Spec文档/2026-08-16-语音桥-spec-v0.4.md`

## 1. 结论

**v0.4 真机固件联调通过**。ESP32-S3-BOX-3 真机「按住说话 → 录音 → 上传 → 流式 ASR → 路由 → LLM → TTS → 播放」全链路跑通，用户实测听到回复。

## 2. 实测链路（固件串口证据）

```
btn PRESS_DOWN -> start recording      ← GPIO0 顶部圆键按下
btn PRESS_UP   -> stop recording       ← 松开
REC END, bytes=106496                  ← 录音 3.3 秒（106496B ÷ 32KB/s）
UPLOAD DONE, bytes=106496              ← 上传完整，零丢失
HTTP status=200                        ← 服务端成功响应
（随后收到长度前缀 WAV 帧并播放）
```

## 3. 交付物清单

| 模块 | 内容 |
|---|---|
| 服务端 | `asr.py` 加 `StreamingASR`（zipformer 流式，partial/final）；新端点 `POST /api/v1/voice/stream`（raw PCM 上传 + 流式 ASR → 路由 → 帧流） |
| 固件 | `voice_agent.c`：ES7210 双麦录音 → 降混 → 上传 → 收帧 → 跳 WAV 头 → 升混 → ES8311 播放 |
| 模型 | `streaming-zipformer-bilingual-zh-en-2023-02-20`（487MB，github release 走代理下载） |

## 4. 排障记录（两个关键坑）

### 坑1 · 按键不触发
- **现象**：按顶部圆键无反应，串口无 `btn` 事件。
- **根因**：BOX-3 顶部圆键 = **GPIO0 = BSP_BUTTON_CONFIG**（非 GPIO1/MUTE）。之前受 chatgpt_demo 用 MUTE 做静音键误导绑错。
- **定位**：加 GPIO 电平诊断（`gpio_get_level` 直读，不依赖 iot_button 事件机制），按顶部圆键时 GPIO0 从 1→0 实锤。
- **修复**：MUTE + CONFIG 双注册。

### 坑2 · PCM 上传 400（协议层）
- **现象**：服务端 uvicorn 报 `Invalid HTTP request`，一个字节 PCM 都没收到。
- **根因**：`esp_http_client_open(client, write_len)` 中 `write_len >= 0` 走 **Content-Length**，只有 `write_len < 0` 才走 chunked。原代码 `open(client, 0)` 实际发 `Content-Length: 0` 却往 body 写数据 → 协议冲突。且 esp_http_client 的 chunked 请求上传**不做自动 chunk 编码**（write 是 raw write）。
- **修复**：改为**先录音到 PSRAM（512KB=16s）→ 松开后用 Content-Length 一次性上传**（`open(client, total)` + write）。LAN 下 opus 无收益、PCM 直传最可靠，服务端 `request.stream()` 兼容。

## 5. 遗留

1. **延迟计量待补测**：本次真机联调服务端结构化日志（open_ms / llm_ttft / route）因服务进程重启丢失，功能链路已通但延迟数据需复测一轮补记。
2. **流式 ASR 真机准确率**：zipformer 流式模型（替代 SenseVoice）对真机口语的识别准确率需多轮采样评估。
3. **固件 DIAG 日志清理**：GPIO 电平诊断日志（每电平变化一条）待移除。
4. `rag_score_threshold=0.0` 过松（v0.4 服务端已知，需调阈值）。

## 6. 结论

v0.4 Spec（A1 raw PCM / A2 流式 ASR / A3 按键触发）全部落地并经真机验证。核心目标「真机固件联调」达成，可交 Hermes 复审。
