# v0.4 真机固件联调自测报告

> 日期：2026-08-16 · 版本：voice-bridge v0.4 + voice_agent 固件 · 依据：`规划文档/Spec文档/2026-08-16-语音桥-spec-v0.4.md`

## 1. 结论

**v0.4 真机固件联调通过，A1~A6 全部落地**。ESP32-S3-BOX-3 真机「按住说话 → 边录边传（chunked 流式）→ 流式 ASR → 路由 → LLM → edge TTS → 帧播放」全链路跑通，用户实测听到回复。

## 2. 实测链路（固件串口 + 服务端日志证据）

```
btn PRESS_DOWN → REC+UPLOAD START(chunked)   ← GPIO0 顶部圆键，边录边传
（录音期间逐块上传 PCM，服务端 ASR partial 持续更新）
btn PRESS_UP → 流结束（0 长度 chunk）→ ASR final
→ 路由(lightweight) → DeepSeek → edge 合成 → 帧 → 播放
HTTP 200  OK
```

## 3. 交付物清单

| 模块 | 内容 |
|---|---|
| 服务端 ASR | `StreamingASR`（zipformer 流式，partial/final）；`/voice/stream` 端点边收边识别，奇数字节缓存 |
| 服务端 TTS | **edge-tts 7.2.8 唯一**（弃 piper）：mp3 → miniaudio 解码重采样 16k → WAV；故障报 502 |
| 服务端间隔 | splitter 小数点消歧（数字.数字不拆句）+ pipeline 句间 300ms |
| 固件 | `voice_agent.c`：ES7210 双麦录音 → 降混 → **chunked 手动编码流式上传** → 收帧 → 播放 |
| 模型 | `streaming-zipformer-bilingual-zh-en-2023-02-20`（487MB） |

## 4. 排障记录（五个关键坑）

### 坑1 · 按键不触发
- 根因：BOX-3 顶部圆键 = **GPIO0 = BSP_BUTTON_CONFIG**（非 GPIO1/MUTE）。
- 修复：MUTE + CONFIG 双注册。

### 坑2 · chunked 上传语义（open write_len）
- 根因：`esp_http_client_open(client, write_len)` 中 `write_len >= 0` 走 Content-Length，`write_len < 0`（即 -1）才走 chunked。且 write 是 raw write，不做自动 chunk 编码。
- 修复：`open(client, -1)` + 手动 chunk 编码（size 十六进制行 + 数据 + CRLF，结束 `0\r\n\r\n`）。

### 坑3 · WiFi 2702 认证失败 reason=2
- 现象：密码正确（PC 保存确认）但 `STA disconnected reason=2`（AUTH_EXPIRE）。
- 根因：认证模式协商问题（2702 auth=3 纯 WPA2，L1122S auth=4 混合却能连）。
- 修复：显式 `wc.sta.threshold.authmode = WIFI_AUTH_WPA2_PSK` + `wc.sta.channel = 11` + FAST_SCAN。

### 坑4 · 服务端奇数 chunk 崩溃
- 现象：`buffer size must be a multiple of element size`（收到 1435 奇数字节）。
- 根因：TCP/chunked 分包可在奇数边界切分 int16 PCM，`np.frombuffer(chunk, int16)` 要求偶数。
- 修复：`_feed` 加 leftover 字节缓存（奇数字节拼接下一 chunk）。

### 坑5 · 「先录音后上传」的流式妥协（已纠正）
- 初版为解决 chunked 坑改成了「先录音到 PSRAM 再 Content-Length 上传」，违背 Spec A2 流式（录音期间服务端空闲，松开才识别）。
- 纠正：恢复真正流式（坑2 方案），边录边传、录音即识别。

## 5. 延迟计量（真机实测，服务端落盘日志）

| 阶段 | 语音 | open_ms | llm_ttft | tts_first | 说明 |
|---|---|---|---|---|---|
| piper 时代（稳态） | 「你刚刚是不是卡住了」 | 989ms | 584 | 198 | 快但音色差 |
| edge 时代（流式恢复后） | 「你」 | **2416ms** | 1134 | 1175 | edge 音质代价 |

- 流式恢复后用户感知延迟（松开 → 首帧）≈ open_ms 2416ms，ASR 已边录边传完成、不计入等待。
- edge 首音频 1175ms 是 Vange「小智音质」决策的固有代价（Spec §9 已接受）。
- llm_ttft 1134ms 偏慢（DeepSeek 冷连接，暖机后 ~600ms）。

## 6. 收尾项（已闭环）

1. ✅ `rag_score_threshold` 0.0 → **1.0**（BM25 分数实测切分）。
2. ✅ 固件 DIAG 日志移除。
3. ✅ A5 edge 唯一 + A6 间隔（小数点消歧 + 300ms）实现，26 passed。
4. ⏸ 流式 ASR 真机准确率需多轮采样（zipformer 替代 SenseVoice）。

## 7. 结论

v0.4 Spec A1~A6 全部落地并经真机验证：raw PCM（A1）、流式 ASR 边录边传（A2）、按键触发（A3）、edge 唯一（A5）、间隔优化（A6）。可交 Hermes 复审。
