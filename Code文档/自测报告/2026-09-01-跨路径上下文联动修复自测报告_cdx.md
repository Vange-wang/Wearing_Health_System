# 2026-09-01 跨路径上下文联动修复自测报告

## 1. 结论与状态

- 本地实现与自动化自测：**PASS**。
- Hermes 独立代码审查 R3：**PASS**（SERIOUS 0 / NON_SERIOUS 0；OI-1/OI-2/OI-3 全部 CLOSED）。
- BOX-3 真机语音与全链路首字门：**PASS**；设备侧 N=10 中位数 `1391.754ms < 1484.589ms`。
- ISSUE-0013：**保持 Open，不在本报告中关闭**。
- 本报告同时引用 2026-09-01 BOX-3 真机验收证据；不等价于提交或推送，且在 Hermes 最终关闭裁决前 ISSUE-0013 仍保持 Open。

## 2. 依据与边界

- Spec：`规划文档/Spec文档/2026-09-01-跨路径上下文联动修复-spec_hm.md`，SHA-256 `9A16D37492F95AF0FF1D9A6361F989E551DDB83BA215A29348F9BE5568B67C14`。
- 任务单：`协同工作文档/codex_tasks/2026-09-01-跨路径上下文联动修复-任务单_hm.md`，SHA-256 `B99EE86574CF9C794EE8AD9E532C41690F612EFBBF9E53342B3EB7C7C65C3C7B`。
- 实施日期：2026-09-01（Asia/Shanghai）；证据收口时间：2026-09-01 14:07:57 +08:00。
- 实施对象：`D:\workbuddy_project\项目\可穿戴健康辅助系统\voice-bridge`。
- 未修改固件、MAX30102、BLE 8 字节帧、告警质量门、TTS/ASR、`llm.py`、配置、依赖或 API；未烧录、重启、采样、提交、推送。

## 3. 基线冻结

- Git HEAD：`ff3ab942997b46a5beffb4daa76a66572adba7ef`（实施前后未变）。
- 实施前主目录全量：`172 passed, 5 skipped, 1 warning in 75.55s`。
- 既有未跟踪文件：26 个，逐文件 SHA-256 已冻结；实施后复核结果 `PREEXISTING_NON_WHITELIST_HASHES_UNCHANGED count=26`。
- 白名单文件实施前哈希：
  - `router.py`：`5BE29112BB7F3D7982A7EF31166F010FDD77C1E0BBE7DC814D331259921A8B95`
  - `pipeline.py`：`F7BFCF33293C5B7EAB6241EA4DD88B5C57D5263390CECA07E46821B5085BC60F`
  - `test_router.py`：`2E80430E471A85FEE02315E6E339A7CD275FEF42A5224C1BE2D6067F5F1DB779`
  - `test_pipeline.py`：实施前不存在。

## 4. TDD 证据

1. Router 分类 RED：新增公共接缝 `Router.classify_followup()` 测试后，结果 `3 failed, 10 passed`，失败原因为方法不存在。
2. Router GREEN：实现复合短语分类后 `13 passed`；既有 ISSUE-0010 `_is_followup` 未改。
3. Pipeline REPLAY/无快照 RED：`3 failed`，原实现均落到 `lightweight`。
4. Pipeline REPLAY/无快照 GREEN：有效快照本地重播；无快照走固定诚实模板；不调用 RAG/LLM。
5. CONTEXT 四路 RED：有效 CONTEXT 仍落回原路由，且有效 REPLAY 后端标记错误，共 `6 failed, 2 passed`。
6. CONTEXT 四路 GREEN：LIGHTWEIGHT/HERMES 单次调用并带固定前缀；RAG 不二次检索；DATA 本地重报最新新鲜字段。
7. 失败语义 RED：普通轮 TTS 失败曾错误覆盖快照，测试结果 `1 failed`。
8. 失败语义 GREEN：仅完整成功交付后提交快照；TTS/LLM 中断和取消保留旧快照。
9. CONTEXT 轻量降级 RED/GREEN：先证明降级 Hermes 丢失上下文前缀，再修为保留同一固定前缀。

## 5. 行为矩阵

| 场景 | 实测结果 |
|---|---|
| 8 个 REPLAY 复合短语 | 均分类为 `replay` |
| 6 个 CONTEXT 复合短语 | 均分类为 `context` |
| 6 个普通负例 | 均为 `None` |
| 有快照 REPLAY | 重播上一轮完整回复；`timing.route=replay`；`llm_backend=replay`；无第二次 LLM/RAG |
| 无快照/TTL 过期 REPLAY | 固定本地诚实模板；无网络/LLM |
| 无快照 CONTEXT | 固定本地诚实模板；无网络/LLM |
| CONTEXT·LIGHTWEIGHT | 单次 lightweight，固定上一轮前缀，reply 最多 500 字 |
| CONTEXT·HERMES | 单次 Hermes，固定上一轮前缀 |
| CONTEXT·RAG | 不二次 `rag.search`，不二次包装知识库前缀，单次 lightweight |
| CONTEXT·DATA | `_build_health_reply("")` 重报所有最新新鲜字段；无新鲜值时诚实降级；零 LLM |
| CONTEXT·DATA 降级后 REPLAY | DATA 旧值 72/98 失效后，CONTEXT 覆盖为诚实降级文本；后续 REPLAY 仅复述降级文本，不含旧值 72/98；零 LLM |
| TTL | `now-last_ts > 600` 视为无快照 |
| REPLAY 刷新 | 不覆盖、不刷新时间戳 |
| CONTEXT 成功 | 覆盖成唯一下一轮快照，并保留继承的原始 route |
| TTS/LLM 中断、取消 | 不写脏快照 |
| 两请求并发 | 最终快照为一个完整 user/reply 对，无字段撕裂 |

## 6. 自动化测试

- 定向受影响回归：`74 passed, 1 warning in 21.59s`。
- R1 后定向（Pipeline + Router）：`32 passed in 1.62s`。
- R1 后主目录全量：`194 passed, 5 skipped, 1 warning in 79.52s`。
- OI-3 澄清后定向（Pipeline + Router）：`33 passed`（隔离工作区 `1.90s`；主目录 `1.61s`，主目录受沙箱缓存写权限影响额外出现 1 条非代码 warning）。
- OI-3 澄清后主目录正常权限全量：`195 passed, 5 skipped, 1 warning in 85.49s`。
- 相对基线：新增 23 个测试；失败为 0；skip 仍为 5；正常权限 warning 仍为 1，没有新增失败、跳过或警告。
- 唯一 warning 为既有 Starlette/httpx 弃用提示，本任务未引入。
- 隔离工作树曾因缺少主目录 `.env` 中的 `WECHAT_CHAT_ID` 在收集阶段出现 5 个配置错误；该次不作为回归结论。随后在权威主目录原环境完成上述全量 PASS。

## 7. 首字/首帧时延证据

测试方法：内存 Fake、零网络、零硬件，N=10，记录从 `run_text()` 开始到首帧 yield 的 wall-clock；统计 median 与 p95。

| 路径 | 修改前 median / p95 (ms) | 修改后 median / p95 (ms) |
|---|---:|---:|
| 普通 LIGHTWEIGHT | 0.273 / 0.782 | 0.423 / 0.697 |
| HERMES comfort | 0.137 / 0.155 | 0.208 / 0.258 |
| DATA | 0.178 / 0.208 | 0.256 / 0.307 |

- 三条普通路径的中位数变化绝对值不超过 0.150ms，p95 变化绝对值不超过 0.103ms，属于该微秒级 Fake 测量的亚毫秒波动；未观察到可感知首字退化。
- 同文本普通 LIGHTWEIGHT：median `0.428ms`，p95 `0.464ms`。
- REPLAY：median `0.260ms`，p95 `0.318ms`；额外 LLM 调用 `0`。
- 本地门：REPLAY median/p95 均低于普通路径，满足 `REPLAY <= 普通路径 + 50ms`。
- BOX-3 真机固定口令“你好”设备侧有效 N=10：`1374.009, 1744.740, 1140.030, 1409.498, 1416.848, 1353.303, 2171.783, 1873.534, 1310.093, 1083.183ms`。
- 真机中位数 `1391.754ms`，比冻结红线 `1484.589ms` 快 `92.835ms`；线性插值 P95 `2037.571ms`，最大值 `2171.783ms`，普通 lightweight 路径主门无回退。
- 行为真机门：生日→REPLAY 再答出相同生日，`llm_backend=replay`、无二次 LLM、无 comfort；Hermes 天气搜索→CONTEXT 指代追问继续回答天气。
- 详细逐轮表、无效/冗余样本与原始日志见 `协同工作文档/验收与澄清/2026-09-01-ISSUE-0013-BOX3真机语音与首字验收_cdx.md`。

## 8. 实施后白名单文件哈希

- `voice-bridge/app/router.py`：`35371BA2402EEFC035BC2B0B5CADBE0777402DE0849F9AC0FC27D8B6F68F3724`
- `voice-bridge/app/pipeline.py`：`E27684BD229D7E567C2F20FC45CD14377AC9CD2118CA052BB37ECF7AFAE967FE`
- `voice-bridge/tests/test_router.py`：`E4288F3716653AFB2DA4F6F3B1BC5167880CF519B6D2FC755E9C2020656CC522`
- `voice-bridge/tests/test_pipeline.py`：`123EADCD696A79D38DFD313159B419D707F83FC240290B4AE07923315B5B4A19`

## 9. 未完成门与唯一下一动作

- Hermes R1 报告：`协同工作文档/审查报告/2026-09-01-跨路径上下文联动修复-实现与自测-审查-第1轮_hm.md`，verdict=`PASS_WITH_NONBLOCKING_OPEN_ISSUES`，SHA-256 `4DB0791B7CE2FBE4799FF9A9C4C98E94C28B358A6B7427072BD55DA6346B2773`。
- Hermes R2 报告：`协同工作文档/审查报告/2026-09-01-跨路径上下文联动修复-实现与自测-审查-第2轮_hm.md`，verdict=`PASS_WITH_NONBLOCKING_OPEN_ISSUES`，SHA-256 `C0AEDADF02AE537CF959D559E3A38EF22812FB01CFDD58ADCACD1D8126F234EB`。
- OI-1 已用 `comfort_sent is False` 显式断言关闭；OI-2 已用流式 TTS 中途 `TTSError` 失败注入关闭。
- OI-3 已由 Vange 选择“保持当前覆盖”：DATA 无新鲜值的成功 CONTEXT 诚实降级轮覆盖快照并刷新时间戳，后续 REPLAY 复述诚实降级文本而非过期健康值；现有业务代码已符合，未修改 `router.py`/`pipeline.py`，仅补一条验收测试。
- Hermes R3 报告：`协同工作文档/审查报告/2026-09-01-跨路径上下文联动修复-实现与自测-审查-第3轮_hm.md`，verdict=`PASS`，SERIOUS 0 / NON_SERIOUS 0，SHA-256 `F1431A473BE2BCCAD712F758F0830C572CCE62965ACBA2DA80CEE15A337DF9DF`。
- BOX-3 真机验收已通过；验收过程中发现 8710 首次仍为源码更新前启动的旧进程，先完整保留旧缺陷复现，再按精确 PID 所有权检查重载项目服务，正式样本只取新进程结果。
- 当前状态：`LOCAL_SELFTEST_PASS / HERMES_R3_REVIEW_PASS / HARDWARE_ACCEPTANCE_PASS / HERMES_FINAL_ISSUE_CLOSURE_PENDING / ISSUE_OPEN`。
- 唯一下一动作：把真机报告和原始证据交既有 Hermes session `20260901_004431_11dfd7` 最终核验；通过后由 Hermes 将 ISSUE-0013 从 Open 移入 Close。
