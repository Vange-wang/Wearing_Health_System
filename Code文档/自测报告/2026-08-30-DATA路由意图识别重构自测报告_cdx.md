# DATA 路由意图识别重构自测报告

- 执行人：Codex（开发员）
- 执行时间：2026-08-30 至 2026-08-31（Asia/Shanghai）
- 对应任务单：`协同工作文档/codex_tasks/2026-08-30-DATA路由意图识别重构-任务单_hm.md`
- 对应计划：`协同工作文档/codex_tasks/2026-08-30-DATA路由意图识别重构-实施计划_cdx.md`
- 当前状态：**READY_FOR_HERMES_REVIEW**
- 边界：本报告是开发员自测，不代替 Hermes `REVIEW_PASS`

## 1. 实施结果

1. DATA 从裸字段子串匹配收窄为明确的当前数值查询短语；知识、原因、关系、异常判断和选购问题不会再被 DATA 抢走。
2. RAG 判定保持在健康领域 lightweight 兜底之前；未命中 RAG 的健康语义问题留在 DeepSeek，不因“看看/是什么”等宽工具词进入 Hermes。
3. `StreamingPipeline` 向 `LightweightLLM` 绑定只读内存健康上下文；心率与血氧逐字段按生产门限 60 秒判断新鲜度。
4. 健康语义问题只可引用提供器给出的新鲜值；过期或缺失时明确禁止猜测。血压当前值仍答未接入。
5. 普通“你好”不调用健康提供器，公共系统提示词与改动前 HEAD 完全相同（换行归一后 488 字、内容一致）；健康三态长规则只在健康问题中追加。
6. ASR 归一新增 `雪阳/血阳/学养/学样 → 血氧`；真实真机语料“雪阳是多少”据此修复。
7. 真机发现并修复“上一轮 DATA 后说你好仍继承 DATA”的会话路由缺陷；问候现在显式切回 lightweight。
8. Hermes `CONDITIONAL_PASS` 指出的单字段 SERIOUS 已整改：单问心率只报心率，单问血氧只报血氧，显式双字段才双报；无字段 DATA 追问保持既有双字段兼容。

## 2. 文件范围

本任务代码与测试文件：

- `voice-bridge/app/router.py`
- `voice-bridge/app/pipeline.py`
- `voice-bridge/app/llm.py`
- `voice-bridge/config.yaml`
- `voice-bridge/tests/test_data_routing_intent.py`
- `voice-bridge/tests/test_rag.py`
- `voice-bridge/tests/test_health_alerts.py`

本任务证据文档：

- `协同工作文档/codex_tasks/2026-08-30-DATA路由意图识别重构-任务单_hm.md`
- `协同工作文档/审查报告/2026-08-30-DATA路由意图识别重构任务单-审查意见_zc.md`
- `协同工作文档/codex_tasks/2026-08-30-DATA路由意图识别重构-实施计划_cdx.md`
- 本报告
- `协同工作文档/完成报告/2026-08-30-DATA路由意图识别重构-完成报告_cdx.md`

任务单明确排除的三份无关并行文档未修改、未夹带。`D:\esp-box` 固件未修改、未构建、未烧录。

## 3. 测试驱动证据

| 阶段 | 失败证据 | 最小修复后 | 说明 |
|---|---:|---:|---|
| 路由意图红灯 | 4 failed, 15 passed | 28 passed | 正常血压、关系问句和“帮我看看心率”原先误路由 |
| 健康上下文红灯 | 8 failed, 25 passed | 33 passed | 提供器、绑定和三态尚未实现 |
| 真机 ASR `雪阳` 回归 | 1 failed, 26 passed | 27 passed | 只增加已观测同音映射 |
| DATA 后“你好”回归 | 1 failed | 11 passed（该用例 + router 套件） | 精确复现会话继承缺陷 |
| 普通提示词零增长 | 1 failed | 30 passed（相关套件） | 将健康长规则从公共提示词移到健康问题条件分支 |
| 单字段回复粒度 | 1 failed | 1 passed（最小用例）；相关 61 passed | 原模板无条件追加心率/血氧，现按文本字段裁剪 |

最终正式命令在 `voice-bridge` 目录执行：

```powershell
.\venv\Scripts\python.exe -m pytest tests -q
```

最终结果：**172 passed, 5 skipped, 1 warning，退出码 0**。五个 skip 是既有真实资源门；warning 是既有 FastAPI TestClient/httpx2 迁移提醒。

直接运行无范围的 `pytest -q` 会额外收集根目录人工脚本 `test_e2e_sim.py::test_voice`，因 `pcm` 是脚本参数而非 pytest fixture，结果为 **172 passed, 5 skipped, 1 warning, 1 collection error**。该人工脚本不在本任务 File Map，规范套件仍为 `pytest tests`，本轮未越界修改它。

## 4. 路由与上下文矩阵

| 输入类型 | 示例 | 结果 |
|---|---|---|
| 当前值 | `我的心率多少`、`血氧是多少` | DATA/template |
| 双字段当前值 | `心率跟血氧`、`你再看一下` | DATA/template，后者走 DATA 会话延续 |
| ASR 近音 | `我的血阳是多少`、`雪阳是多少` | 归一后 DATA/template |
| 未接入当前值 | `血压多少` | DATA/template，明确未接入 |
| 知识/原因 | `心率的正常范围是多少`、`心率为什么突然变快` | RAG 或 lightweight/DeepSeek |
| 关系/选购 | `心率跟血氧是什么关系`、`血氧仪怎么选` | RAG 或 lightweight/DeepSeek，不进 DATA |
| 白名单外语义报数 | `帮我看看心率` | lightweight/DeepSeek，只引用新鲜上下文 |
| 数据过期 | `帮我看看心率` | lightweight 明确无最新值，不复述旧值 |
| 数据过期 | `心率多少` | DATA/template 明确检测中断 |
| 普通问候 | DATA 后紧接 `你好` | lightweight 正常问候，不继承 DATA |

无网络集成测试使用假 LLM/TTS 和内存 `HealthDataStore`，未访问外网；验证新鲜双字段、单字段新鲜、全部过期、DATA/template 和 lightweight 提示词边界。

## 5. 静态与性能边界

- Router 相同语料 10,000 次：改动前中位数 3.100 μs、P95 4.700 μs；改动后中位数 2.800 μs、P95 5.700 μs。中位数改善 0.300 μs，P95 增加 1.000 μs，均为微秒量级且无 I/O。
- `_system_prompt("你好")`：健康提供器调用 0 次；公共提示词与 HEAD 内容相同。
- 健康问题：提供器每次调用 1 次，只读内存；无新增文件读、网络、线程、定时器或第二次 LLM 调用。
- `requirements.txt`、`requirements-lock.txt` 无变化；DeepSeek 继续 `trust_env=False` 直连。
- `git diff --check -- voice-bridge/app voice-bridge/tests voice-bridge/config.yaml` 退出码 0。
- Git 暂存区为空；未 commit、未 push、未 merge、未 reset、未清理工作树。

## 6. 真机 DATA 与语义验收

现场：同一 BOX-3、同一现场网络、腕带经 BOX-3 持续上报，voice-bridge 为 required 认证；未烧录固件。

确定性 DATA：

| 输入 | 路由/后端 | 现场结果 |
|---|---|---|
| 我的心率多少 | DATA/template | 73/99 等新鲜值 |
| 血氧是多少 | DATA/template | 初次验收为新鲜值；当时模板仍同时播报心率和血氧，后续已按 Hermes S1 整改 |
| 心率跟血氧 | DATA/template | 新鲜双字段 |
| 你再看一下 | DATA/template | DATA 会话延续 |
| 我的血阳是多少 | DATA/template | 归一成功；一次微动心率 116，随后恢复 |
| 血压是多少 | DATA/template | “血压功能还没有接入” |

语义回答：

- `帮我看看心率`：RAG/DeepSeek，引用同一时段新鲜心率 72，首字 2143 ms。
- `心率的正常范围是多少`：RAG/DeepSeek，回答 60–100 次/分钟并结合当前值，首字 1435 ms。
- `心率为什么突然变快`：ASR 有漏字但正确进入 RAG，回答运动/情绪/睡眠等原因，首字 1831 ms。
- `心率跟血氧是什么关系`：ASR 漏字但未进 DATA，回答关系并引用新鲜值，首字 1942 ms。
- `血氧仪怎么选`：多次真机 ASR 误成“血氧乙怎么”“这样一怎么选”“我想去转移应该怎么选择”；服务通过会话上下文给出选购答复且未进 DATA，但独立 ASR 语义未通过，列为残余风险。

过期保护：移开手指并等待超过 60 秒后，`帮我看看心率` 明确“没有最新心率数据”，未引用旧值；`心率多少` 走 DATA/template，明确“健康数据检测暂时中断”，首字 1253 ms。

### 6.1 Hermes CONDITIONAL_PASS 后单字段整改

Hermes 审查报告 `协同工作文档/审查报告/2026-08-31-DATA路由意图识别重构完成报告-审查裁决_hm.md` 将字段粒度列为唯一 SERIOUS。整改后真机复测：

| 输入 | 实际回复 | 路由/后端 | 首字 |
|---|---|---|---:|
| 我的心率多少 | 您当前心率 63，正常。 | DATA/template | 1398 ms |
| 血氧是多少 | 您当前血氧 99。 | DATA/template | 1334 ms |
| 心率跟血氧 | 您当前心率 66，正常，血氧 99。 | DATA/template | 1293 ms |

三条 ASR 均完整准确；单字段回复未夹带另一字段，双字段仍完整，首字均未回退。自动化先红后绿，相关套件 61 passed，正式套件仍为 172 passed、5 skipped、1 warning。

## 7. 首字延迟红线

### 7.1 普通 lightweight：`你好`

首次候选 5 轮为 `2066、1854、1406、1385、1611 ms`，中位数 1611 ms，高于冻结基线 1484.589 ms，按计划追加到 10 轮。诊断发现公共系统提示词对普通请求也携带健康长规则，违反“普通请求不增加 token”；先写失败测试，再把健康规则移到条件分支。

修正后重新启动服务并采 10 个有效样本：

`1360、1391、1397、2199、1284、1865、1457、1551、1496、1312 ms`

- 中位数：**1427 ms**，比冻结基线 1484.589 ms 快 57.589 ms，主门禁通过。
- 插值 P95：2048.7 ms；样本最大值 2199 ms。
- 变异系数：17.77%；极差 915 ms。相较冻结批次 16.06% / 619.618 ms 仍有外部 LLM/TTS长尾，需 Hermes 复核其可接受性。

### 7.2 确定性 DATA：`我的心率多少`

改动前同路径 5 轮首字：`1526、1196、1525、1325、1469 ms`，中位数 **1469 ms**。

最终候选 5 个完整固定口令样本：`1949、1460、1220、1167、1514 ms`，中位数 **1460 ms**，比基线快 9 ms；全部为 DATA/template，没有首字回退。1514 ms 样本在手指漂移时心率为 104，只用于确定性模板延迟，不作为生理准确性证据。

## 8. 证据日志

- 冻结完整服务日志：`C:\Users\86166\AppData\Local\Temp\data-routing-20260831-cdx\voice-bridge-live-final.log`
- 字节数：2,942,391
- SHA-256：`D2F334BBE3EA05375FEE57A7AF9B002270475703A13F8E4AE10BA41D6D4502EC`
- 原运行日志：`D:\workbuddy_project\项目\可穿戴健康辅助系统\voice-bridge\logs\voice-bridge.log`
- 单字段整改冻结日志：`C:\Users\86166\AppData\Local\Temp\data-routing-20260831-cdx\voice-bridge-single-field-remediation.log`
- 单字段整改日志字节数：2,982,386
- 单字段整改日志 SHA-256：`DA0FD674214DC557707BB52443D5DA9288C382F98370A1C4271D6821E5982FE2`

冻结副本包含 ASR final、route、回复流、`stream_done`/`request_done` 和健康上报 HTTP 证据。原运行日志在服务继续运行时会增长，因此复核应以冻结副本及其哈希为准。

## 9. 残余风险与待裁决项

1. **传感器接触漂移**：微动曾出现心率 111/116/104，重新放稳后恢复到 65–78，血氧 97–99。属于既有 P1+4.6mA/接触风险，不应把漂移值当作医疗结论。
2. **ASR 识别**：`雪阳` 已按真实语料修复；“血氧仪怎么选”仍出现不可稳定归一的整句错识别，不能靠继续穷举替换安全解决。
3. **健康知识准确性**：真机答复曾说“血氧一般会同步变化”，表述过度简化。路由正确不等于医学内容已审核，后续需健康内容校准。
4. **普通通道长尾**：主中位数通过，但 10 轮最大值 2199 ms，P95/极差仍受外部 DeepSeek/Edge TTS 波动影响。
5. 本系统不是医疗器械；现场对照与路由验收不构成诊断准确性声明。

## 10. 自测结论

代码、正式测试、真机路由、过期保护、两条首字主中位数门和 Hermes S1 单字段整改已完成；所有已知失败与排除样本均已保留。当前状态仅为 **READY_FOR_HERMES_REVIEW**。在 Hermes 复核明确 `REVIEW_PASS` 且 Vange 另行授权之前，不提交、不推送。
