# 微信接收标识迁移与暴露范围

- 执行人：Codex（开发员）
- 复核日期：2026-08-30
- 记录原则：只记录键名、存在性、数量和哈希，不记录接收标识原值

## 1. 当前状态

| 项 | 结果 |
|---|---|
| 跟踪键名 | `wechat_chat_id` |
| 本地环境键名 | `WECHAT_CHAT_ID` |
| 当前跟踪配置是否含非空值 | 否；`voice-bridge/config.yaml` 仅保留空值和环境变量说明 |
| 本地 `.env` 是否存在非空值 | 是；文件被 Git 忽略，未输出原值 |
| 当前 `config.yaml` SHA-256 | `F029CC6F5BAC9912DA05219D2CD37313AAA0CC2E70DD3FD05EECA05D702E0F2A` |

启动配置会在启用微信推送但环境键缺失时失败，不再退回跟踪配置中的真实接收标识。运行日志只记录告警 ID、错误类别、重试时刻和成功计数，不记录完整接收标识。

## 2. Git 历史暴露范围

- `voice-bridge/config.yaml` 历史触达提交数：**16**。
- 含非空 `wechat_chat_id` 的提交数：**3**。
- 受影响提交短哈希：`7e1672d4ea51`、`79ca7012a5d7`、`91edbc11cc4b`。
- 当前工作树跟踪文件非空值：**不存在**。

复核脚本逐提交读取 `voice-bridge/config.yaml` 并只判断键值是否为空；没有把历史值写入命令输出、报告或日志。

## 3. 处置建议与授权边界

1. 在微信/iLink 对应平台侧轮换该接收标识或相关绑定，轮换后的值只写入本地 `voice-bridge/.env` 的 `WECHAT_CHAT_ID`。
2. 轮换后重启 voice-bridge，并以一条受控告警验证发送成功和成功计数；不得通过绕过配额实现测试。
3. Git 历史中的 3 个提交仍含旧值。**历史重写需要单独授权**，并可能涉及远端强制推送和所有协作者重新同步，本轮未执行。
4. 本轮未 commit、未 push、未删除历史、未修改远端平台配置。

